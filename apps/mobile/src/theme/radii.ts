/**
 * Border-radius tokens for the mobile design system.
 * Matches the .pen design file variables.
 */
export const radii = {
  /** 4px — small inner elements */
  xs: 4,
  /** 6px — form fields, small cards */
  sm: 6,
  /** 8px — standard cards, inputs */
  md: 8,
  /** 12px — large cards, modals */
  lg: 12,
  /** 16px — hero sections, large containers */
  xl: 16,
  /** 9999px — pills, fully rounded */
  pill: 9999,
} as const;
