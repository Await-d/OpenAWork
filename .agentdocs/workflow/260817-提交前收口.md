# 提交前收口

## 任务概览
在不删除、跳过或削弱失败测试的前提下，收口当前跨模块未提交改动：确认生产缺陷、修复可证实问题、按行为拆分提交，并在推送前给出基于证据的放行结论。

## 当前分析
- 工作树包含 127 个已跟踪改动和 4 个未跟踪文件，涉及 opencode-llm、Gateway、Web、agent-core 与共享包。
- 根质量门禁已通过 typecheck、lint、format:check、build、Fastify 对齐检查与 diff 检查；Gateway 单测与验证矩阵仍有失败。
- 失败测试不可通过删除或削弱处理；仅修复已经复现并证明属于生产代码的错误。

## 复杂度评估
- 原子步骤：5 个以上 → +2
- 可并行审计流：生成物契约、会话删除、跨平台测试 → +2
- 涉及模块/系统：5 个以上 → +1
- 单步时长超过 5 分钟：是 → +1
- 需要保留审核证据：是 → +1
- OpenCode 原生任务系统：否 → 0
- **总分：7**
- **选定模式：完整编排**
- **路由理由：** 改动体量大且主题混合，必须以可验证行为拆分，避免把测试环境问题或生成物噪声混入生产提交。

## 实施计划

### 第一阶段：边界与根因
- [ ] T-01：核对分支、工作树、生成物与提交分组边界。
- [ ] T-02：复现 session tree 删除验证器失败，确认生产路径的任务图根解析时序。
- [ ] T-03：仅对确认的生产根因实施最小修复并完成定向验证。

### 第二阶段：测试与提交
- [ ] T-04：分类 Gateway 失败项，修复生产失败并记录仅测试夹具/平台断言阻塞。
- [ ] T-05：按模块与行为精确暂存、逐组验证并创建原子提交。
- [ ] T-06：提交后重跑匹配门禁、核对同步状态与推送前条件。

## 记录
- 2026-08-17：已有根门禁成功不等于可推送；完整测试和验证矩阵仍是放行条件。
