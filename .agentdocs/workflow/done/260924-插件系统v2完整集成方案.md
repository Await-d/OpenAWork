# 260924-插件系统v2完整集成方案

## Task Overview

将 OpenAWork 的插件系统从当前的 **V1 风格 hook bus**（`runtime/plugin-host.ts`，346 行，5 个 hook，`OPENAWORK_PLUGINS` 环境变量加载）升级为**对齐 opencode v2.0.15 的完整插件平台**：插件定义模型、域化 hook、Effect/Promise 双 API、生命周期管理、能力域（工具注册/事件/存储/配置变换）、声明式发现、热重载、安装与管理面。

- **参考基线**：`temp/opencode-v2.0.15`（tag `v2.0.15` / commit `6f3639d82e`）
- **本仓现状**：`services/agent-gateway/src/runtime/plugin-host.ts`（2026-09 引入，PR-D-Plugin，MVP 定位）
- **本文档定位**：分阶段可执行方案（T-XX 原子任务 + 验收契约 + Gate），**方案阶段，未开始编码**
- **当前状态**：待 Gate 0 决策

## Current Analysis

### 一、参考库 v2.0.15 插件架构（七层）

| 层 | 位置 | 核心机制 |
|---|---|---|
| SDK（Effect 原生） | `packages/plugin/src/effect/` | `Plugin.define({ id, effect })`；Context 含 20+ 域（session/tool/permission/provider/model/agent/command/event/mcp/skill/storage/integration/reference/rpc/vcs/websearch/worktree/aisdk/generate/app/location/options/plugin）；`ctx.tool.hook("execute.before", cb)` 域化注册；`Transform<T>`（tool.transform 可 add/update/remove/namespace）；`ModelHooks` 支持 `providerID` 过滤 |
| SDK（Promise 兼容） | `packages/plugin/src/promise/` | `Plugin.define({ id, setup(ctx) })`，setup 返回 cleanup；`PluginPromise.fromPromise` 适配进 Effect 宿主 |
| Hook 域 | `packages/core/src/plugin/hooks.ts` | 5 域 18 事件：`aisdk.{sdk,language}`、`session.{prompt,context,compaction,generate,title,model.request,http.request,http.response,experimental.ws.*,retry}`、`permission.evaluate`、`shell.create.before`、`tool.{execute.before,execute.after}`；**类型化 failure 通道**（仅 `tool.execute.before` 可失败 → 拒绝工具调用）；`providerID` 选项过滤；注册返回 `{ dispose }` |
| 宿主 | `packages/core/src/plugin/host.ts`（24KB） | `PluginHost.make` 构造全部域 API；hook 注册/触发；`storage(kv, plugin.id)` 作用域化；RPC 事件桥 |
| 注册表 | `packages/core/src/plugin.ts` + `plugin/service.ts` | `Plugin.Service`（activate/list/close/awaitActivation/hold）；Slot/Activation 模型；**前缀 diff**（只重建变更后的插件，revision 比较）；失败降级（保留旧 revision 运行）；`State.group` 变换失败自动禁用；`bus.publish(Plugin.Event.Updated)` |
| 发现/配置/监督 | `config/plugin/source.ts`、`plugin/module.ts`、`plugin/supervisor.ts` | 配置 `plugins` 数组（`{package,options}` / `-target` 移除 / `*` 通配）；目录发现（`.opencode/plugin(s)/` + 全局 config 目录）；npm 安装（`Npm.add`）+ Git spec；**热重载**（Watcher + SHA-256 digest 指纹 + changes 流）；supervisor resolve（pre/post 插件、启用集、失败记录、pending、duplicate ID 处理） |
| 内部插件 | `packages/core/src/plugin/internal.ts` + `config/plugin/*` | 20+ 内置插件全部用插件架构实现（agent/command/provider/policy/skill/mcp/instruction/...）；`guarded` 集合（`OpencodePlugin`、`ConfigPolicyPlugin` 不可被配置移除） |
| 多进程/生态 | `packages/tui/src/plugin/`、`plugin/src/tui/`、CLI、`plugin/update.ts` | TUI 插件（entrypoints.tui）；RPC 插件（entrypoints.rpc，`rpc.*` 事件）；CLI `plugin add/list/remove/update/check/inventory`；更新检查 |

**Entrypoint 约定**（`packages/plugin/src/host.ts`）：包解析 `server` / `tui` / `rpc` 三个子路径，本地目录解析 `index`。

### 二、本仓现状与差距矩阵

| 能力 | 本仓现状 | 差距 |
|---|---|---|
| 插件定义 | ESM default export async factory → 返回 hooks 对象（无 id/无版本/无清理） | 🔴 缺定义模型与生命周期 |
| Hook | 5 个（`tool.execute.before/after`、`chat.message`、`chat.params`、`permission.evaluate`），`(input, output)` 双参可变对象 | 🟡 形状需迁移到域化单事件 + Effect 失败通道 |
| 权限语义 | deny-only 后置裁决（安全设计，deny-first 不变量） | 🟡 待 Gate 决策：是否放开三值 |
| 工具注册 | ❌ 插件不能注册工具 | 🔴 最大能力缺口 |
| 事件订阅 | ❌ | 🔴 |
| 插件存储 | ❌（可复用 `user_settings` 表） | 🔴 |
| 配置变换 | ❌ | 🟡 按需 |
| 加载 | 仅 `OPENAWORK_PLUGINS` 环境变量，启动一次性 `ensurePluginsLoaded()`（`index.ts:569`） | 🔴 缺声明式配置/目录发现/安装/热重载 |
| 热重载 | ❌（注释明确不支持） | 🔴 无 fs 监听基础设施 |
| 生命周期 | 无（加载即永久） | 🔴 缺 Scope/dispose/supervisor |
| 管理面 | Web UI 插件页仅管 6 个内置工具组开关（`plugin-registry.tsx`），非第三方插件 | 🟡 需扩展为真插件管理 |
| CLI | 无命令体系（`cli/` 仅 2 个文件） | 🟡 可选 |
| 内部插件化 | 内置工具组为硬编码开关（`plugin-tool-settings.ts`）；Provider 插件为内置注册（`provider/plugin.ts`） | 🟡 待 Gate 决策范围 |

### 三、可复用的既有基础设施

| 设施 | 位置 | 复用方式 |
|---|---|---|
| Effect 运行时 | `runtime/effect-runtime.ts:33`（`ManagedRuntime.make`）、`v2-runtime/boot.ts` | 新宿主以 Effect Layer 装配，复用既有 runtime |
| Service/Layer 范式 | `services/logger-service.ts`、`config-service.ts`、`v2-runtime/services/*` | 插件宿主/注册表按同范式新建 |
| 事件总线 | `session/sync-event.ts`（`subscribeBusEvents`/`publishBusEvent`）+ `v2-runtime/services/bus-service.ts`（Effect 包装） | `event` 域与 `Plugin.Event.Updated` 落点 |
| KV | `user_settings(id,user_id,key,value)` 表 | 插件 `storage` 域落点（按 `plugin:<id>:<key>` 命名空间隔离） |
| 安装范式 | `packages/skill-registry/src/installers/github.ts`（HTTP + fflate 解压）；LSP `execSync(installCommand)` | 插件安装可复用 skill 模式或 `bun add` |
| 动态工具加载 | `tools/dynamic-tool-loader.ts`（TTL 5min 缓存 + 失效 API） | `tool.transform` 的运行时刷新参照 |
| 工具登记链路 | `tool-definitions.ts` → `TOOL_WHITELIST` → 权限分类（`tool-category-map.ts`）→ 沙箱执行（6 处登记点，guard 测试强制） | 插件注册工具必须走同链路 |
| Hook 落点 | `tool-sandbox.ts:1541/1517/6399`、`stream.ts:2218`、`stream-runner.ts:660`、`run-upstream-generate.ts:315` | 域化迁移的接线点（已就位） |

### 四、关键约束

1. **deny-first 权限不变量**：所有放行/免审批出口经 `gatePermissionDecision` 统一裁决；插件不得绕过沙箱。
2. **工具登记 6 处**：定义可见清单 / 沙箱 register / TOOL_WHITELIST / 权限分类 /（可选折叠）/（可选派生器）——漏登记会静默失效或被 guard 测试拦截。
3. **桌面 sidecar**：网关编译为 `bun build --compile` 二进制，插件加载路径需兼容编译后形态（`import()` 的可用性需验证）。
4. **无 fs 监听 / 无 npm 安装 / 无声明式配置文件**：这三项是热重载与安装面的前置新建项。
5. **移动端/桌面端复用 Web**：管理界面只需做 Web 一处。
6. **禁 `any`/`@ts-ignore`、ESM NodeNext、Zod 边界校验、中文提交**等仓库约定。

## Solution Design

### 总策略

**分层对齐、渐进交付、旧插件不破坏**。按参考库七层架构逐层映射，每阶段独立可交付、独立验收；现有 `OPENAWORK_PLUGINS` 插件在过渡期自动适配（V1 形状 shim），Phase 5 后提供迁移工具与文档。

**目标形态分层映射（本仓落点）**：

| 参考库层 | 本仓目标落点 | 形态适配 |
|---|---|---|
| SDK Effect/Promise | 新建 `packages/plugin-sdk/`（`@openAwork/plugin`） | Effect 4.0.0-rc.117 与参考库同源；Promise 兼容层保留 |
| Hook 域 | `services/agent-gateway/src/plugin/hooks.ts`（新建） | 首批对齐 `tool.*`/`session.*`/`permission.*`；`shell`/`aisdk` 按需 |
| 宿主 | `runtime/plugin-host.ts` 重构 → `plugin/host.ts` | 域 API 按本仓能力裁剪（无 TUI/persistentPty；RPC 见 Gate 决策） |
| 注册表 | `plugin/registry.ts`（新建，Effect Service） | 对齐 Slot/Activation/前缀 diff/revision/失败降级 |
| 发现/配置/监督 | `plugin/source.ts` + `plugin/module.ts` + `plugin/supervisor.ts` | 配置文件形态见 Gate D-6；目录 `.openawork/plugins/` |
| 内部插件 | `plugin/internal/*`（内置插件目录，按 Gate D-8 范围） | 先迁移 Provider 插件与工具组开关 |
| 管理面 | 网关路由 `/plugins/*` + Web `plugins-tab-content` 扩展 | 无 TUI，Web 即管理面；CLI 可选（Gate D-9） |

### 分阶段交付物

| Phase | 内容 | 交付后用户可见行为 |
|---|---|---|
| **P0** | Gate 0 决策（9 项） | 无（拍板） |
| **P1** | SDK 包 + 宿主内核（定义模型/域化 hook/生命周期/双 API） | 无变化（架构升级；旧插件 shim 兼容） |
| **P2** | 能力域（storage/event/tool.transform/配置变换） | 插件可注册工具、订阅事件、持久化状态 |
| **P3** | 发现与加载面（声明式配置/目录发现/热重载/supervisor/状态诊断） | 插件可声明式启用、自动发现、热重载 |
| **P4** | 安装与管理面（安装机制/管理 API/Web UI） | 用户可安装、启停、查看第三方插件 |
| **P5** | 内部插件化 + 生态（内置迁移/示例/文档/迁移工具） | 内部架构对齐；开发者可发布插件 |

### Gate 0 决策项（**需用户拍板，未决前不进入 P1**）

| # | 决策项 | 选项 | 建议 |
|---|---|---|---|
| D-1 | **API 形态** | A. 双轨：Effect 原生 + Promise 兼容（严格对齐参考库）<br>B. 仅 Promise（轻量，偏离参考库） | **A**：Effect 4.0.0-rc.117 已在仓内，双轨是参考库既定形态；Promise 层降低插件作者门槛 |
| D-2 | **权限 hook 语义** | A. 保持 deny-only（当前安全设计）<br>B. 放开 allow/deny/ask 三值（对齐参考库） | **A**：deny-first 不变量是本仓核心安全边界，建议只补 `message`/`resources` 字段形态对齐，不放开授权 |
| D-3 | **插件注册工具** | A. 支持 `tool.transform`（add/update/remove）<br>B. 只支持 update（改写描述/参数），不支持 add | **A**：这是"插件平台"与"拦截器"的分水岭；须过 6 处登记链路 + 沙箱白名单 + 权限默认 `ask` |
| D-4 | **安装机制** | A. `bun add` 安装 npm 包<br>B. 复用 skill-registry 的 HTTP+解压模式（插件包 = zip）<br>C. 仅本地目录（无安装） | **B 或 A**：需验证 sidecar 二进制内 `bun add` 可行性；C 最保守可作为 P4 首版 |
| D-5 | **热重载** | A. 做（fs.watch + digest 指纹，对齐参考库）<br>B. 不做（重启生效，当前行为） | **A**：开发体验核心；无既有基础设施，P3 新建 |
| D-6 | **声明式配置** | A. 新增 `openawork.json` 配置文件（对齐 opencode.json）<br>B. 复用 `user_settings` 的 `plugins` 键（无文件） | **A**：配置文件是插件生态惯例（版本控制/团队共享）；B 作为 UI 写入的存储后端二者可并存 |
| D-7 | **RPC / 跨进程形态** | A. 不做（无 TUI，桌面 sidecar 无此需求）<br>B. 做（供未来桌面端/移动端扩展） | **A 暂缓**：当前无消费场景，列为 P5 后评估项 |
| D-8 | **内部插件化范围** | A. 仅新建插件平台，内部机制不动<br>B. 把内置工具组（image-generation/desktop-control 等）迁移为插件<br>C. 全量（含 Provider 插件、配置变换） | **B 渐进**：先迁移 6 个内置工具组（收益直接：统一 enable/disable 与 UI），Provider 插件保持现状 |
| D-9 | **CLI** | A. 不做（Web UI 即管理面）<br>B. 做网关 CLI 子命令 | **A**：本仓无 CLI 分发渠道，Web 管理面足够 |

### Gate 0 决策记录（2026-09-24，已拍板）

**用户批复：「没问题进行完成」——按建议全部通过（D-1=A / D-2=A / D-3=A / D-4=先本地目录、P4 验证 `bun add` / D-5=A / D-6=A / D-7=A 暂缓 / D-8=B 渐进 / D-9=A）。**

执行期命名偏差记录：
- SDK 包落点定为 `packages/plugin-sdk/`（包名 `@openAwork/plugin-sdk`），与 gateway 内部 `src/plugin/` 目录区分层级，避免同名混淆。
- T-07 执行策略微调：保持 `runtime/plugin-host.ts` 5 个导出函数签名不变、内部换新宿主实现（调用点零改动），以最小化回归面。

### 兼容与迁移策略

1. **旧插件 shim**：P1 提供 V1 形状适配器——`OPENAWORK_PLUGINS` 加载的 `(input, output)` hooks 自动包装为域化单事件注册；5 个旧 hook 名映射（`chat.message` → `session.prompt`、`chat.params` → `session.context`，等）。
2. **环境变量保留**：`OPENAWORK_PLUGINS` 在 P3 后作为配置文件的补充来源（合并语义），不废弃。
3. **失败隔离不变量**：过渡期保持"插件错误不中断主流程"；P1 起 `tool.execute.before` 是否允许抛错拒绝调用由 D-3 决定后按域对齐。
4. **双写窗口**：P3 起配置与 `user_settings` 同步写入，读取以配置文件优先。

## Complexity Assessment

- Atomic steps: 30+（T-01…T-34 跨 6 阶段）→ **+2**
- Parallel streams: yes（SDK 包 / 宿主重构 / 能力域 / 发现加载 / 安装管理 / 内部迁移 六路可部分并行）→ **+2**
- Modules/systems/services: 5+（agent-gateway、新建 plugin-sdk 包、agent-core、web、docs）→ **+1**
- Long step (>5 min): yes（宿主重构、热重载、工具注册链路、sidecar 验证）→ **+1**
- Persisted review artifacts: yes（方案需 Gate 0 评审并归档）→ **+1**
- OpenCode available: yes → **-1**
- **Total score: +6**
- **Chosen mode: Full orchestration**
- **Routing rationale**: 30+ 原子任务跨 5+ 模块、含宿主架构重写与 sidecar 兼容验证等长步骤、需 Gate 0 九项决策与持久化评审；按 skill 规则须 workflow doc + runtime dir + master_plan。

## Implementation Plan

> **执行顺序**：P0 → P1（串行地基）→ P2/P3（可部分并行）→ P4 → P5。
> **Gate 纪律**：D-1..D-9 未决前不进入 P1；每个 Phase 完成后独立验收、独立归档，不半成品交付。
> **文件改动纪律**：共享热点文件（`tool-sandbox.ts`、`tool-definitions.ts`、`index.ts`、`tool-category-map.ts`）的改动按 ≤3 文件分段提交，段间验证。

### Phase 0: Gate 0 决策 —— ✅ 已完成（2026-09-24）
- [x] T-00 ✅: 用户批复「没问题进行完成」——按建议全部通过；决策记录见「Gate 0 决策记录」节

### Phase 1: SDK 包 + 宿主内核（地基，无用户可见行为变化）—— ✅ 已交付（2026-09-24）
- [x] T-01 ✅: `packages/plugin-sdk/`（`@openAwork/plugin-sdk`，目录名与包名偏差见 Gate 0 记录）——package.json（exports 双入口 + effect 4.0.0-rc.117 依赖）、tsconfig 三件套、`src/types.ts`（Cleanup/Disposable/PluginInfo/PluginOptions/AppInfo）
- [x] T-02 ✅: `src/hooks.ts`——5 个 hook 事件类型（tool.execute.before/after、session.prompt/context、permission.evaluate）+ `HookCallback`/`PluginHookEvents`/`PluginHookName`；类型化 failure 通道与 providerID 过滤留待后续阶段
- [x] T-03 ✅: `src/effect.ts`——`define({ id, effect })`，`Effect<void, never, Scope.Scope>`（对齐参考库）
- [x] T-04 ✅: `src/promise.ts`（包主入口）——`define({ id, setup })` + cleanup 返回值
- [x] T-05 ✅: 宿主模块——`src/plugin/hooks.ts`（PluginHooksRegistry：热路径零 Effect、注册顺序、错误隔离、dispose）+ `src/plugin/context.ts`（域 API 构造、注册追踪）；**偏差**：不做 Effect Service，改为类 + 模块级单例（热路径性能 + 接线兼容；Effect 仅在插件激活时进入）
- [x] T-06 ✅: 注册表——`src/plugin/registry.ts`（PluginRegistry：activate/deactivate/list/close；Effect Scope 长生命周期 + Promise cleanup；失败记录 + 注册回滚；duplicate id 拒绝；V1 序号后缀）；**偏差**：前缀 diff / revision / bus 事件留到 P3（P1 无动态重载场景，全量重建语义等价）
- [x] T-07 ✅: 接线迁移——**偏差（已记录）**：保持 `runtime/plugin-host.ts` 5 个导出函数签名不变、内部委托新宿主；调用点（tool-sandbox:1541/1517/6399、stream:2218、stream-runner:660、run-upstream-generate:315）零改动，回归面最小
- [x] T-08 ✅: V1 shim——`src/plugin/v1-shim.ts`：5 个旧 hook 映射（chat.message→session.prompt、chat.params→session.context；tool.* 字段 reshape；permission.evaluate 直通）；`ensurePluginsLoaded` 同时接受 V1 factory 与 V2 define（`isEffectOrPromisePlugin` 判别）
- [x] T-09 ✅: P1 测试与回归——新增 `__tests__/plugin/plugin-registry.test.ts`（6 例：Promise/Effect 激活与卸载、Scope 资源释放、失败隔离、duplicate、清单可见性、V1+V2 顺序）+ `v1-shim.test.ts`（3 例）；存量 6 文件 41 用例全绿；合计 **8 文件 / 50 测试**；gateway typecheck 0 error；ESLint 0 error；Prettier 通过

**P1 验收契约（已验证）**：
1. ✅ `OPENAWORK_PLUGINS` 旧格式插件行为一致——存量 6 文件 41 用例全绿（plugin-host/tool-sandbox-plugin/permission-evaluate-hook/omo-hook-boundary/provider-request-hooks/stream-runner-plugin）。
2. ✅ Effect 插件（`Effect.acquireRelease` 资源在卸载时释放）与 Promise 插件（cleanup 调用）示例通过全部断言。
3. ✅ 插件抛错不中断主流程（失败隔离）；`tool.execute.before` 保持"错误吞掉"语义（插件可注册工具在 P2/D-3 落地）。
4. ✅ 卸载后 hook 不再触发、注册清理、Scope close 释放资源。
5. ✅ 零插件时行为一致——存量回归用例数与结果不变（41/41 通过）。

**P1 踩坑记录**：新建包后必须先在其 `tsconfig.build.json` 构建出 dist，并在消费方补齐**三套配置**（`tsconfig.json` 源码 paths + `tsconfig.typecheck.json` references + `tsconfig.build.json` dist paths/references），否则 `tsc -b` 会把源码纳入消费方项目（TS6059/TS6307 rootDir 错误）；配置不完整时的一次性降级还会把编译产物吐到 `src/`（同 opencode-llm 历史坑，见 AGENTS.md），修复配置后已清理。

### Phase 2: 能力域（插件从"拦截器"升级为"扩展容器"）—— 🔵 主体已交付（2026-09-24）
- [x] T-10 ✅: `storage` 域——`plugin_storage` 表（`plugin_id` 隔离 + 索引）+ `plugin/storage.ts`（get/set/remove/scan；256KiB 上限、LIKE 通配转义、坏 JSON 容错、scan 上限 1000）+ registry 注入；测试 6 例（往返/插件隔离/覆盖与删除/前缀排序/通配转义/上限拒绝）
- [x] T-11 ✅: `event` 域——`plugin/events.ts`（**eager 订阅**（订阅后首次 next 前的事件不丢）、types 前缀过滤、有界队列 1000（超限丢最旧 + 一次 warn）、signal 组合 `AbortSignal.any`）；插件卸载自动 abort（eventSignal），handler 必被 detach；测试 3 例（含卸载联动）
- [x] T-12 ✅: `tool.transform`——`plugin/tool-registry.ts`（注册校验：名字格式/内置冲突（保留名单由 `tool-definitions.ts` 注入）/重复注册；ToolEditor list/get/add/remove；Gateway 定义转换；执行器含错误隔离）+ context 接线 + registry 卸载清理 + `tool-definitions.ts` 合并 + `tool-sandbox.ts`（白名单注册 + 执行分支，位于 `executeGatewayManagedToolImpl` 入口）；**权限天然落 `custom`(ask)**（未映射 → fail-closed，无需改权限表）；测试 5 例（注册/校验/定义合并/执行器/沙箱端到端）
- [x] T-13 ✅（简化合并到 T-12）: 运行时刷新——沙箱与工具定义**每回合构建**，注册表变化天然在下一回合生效，无需版本指纹/缓存失效；原计划的 transform 运行时刷新机制取消
- [x] T-14 ✅: 配置变换域（agent/provider transform）——**明确不做（评审结论）**：本仓 agent/provider 配置是「DB 行 + 路由 CRUD」形态（`routes/agents.ts` / `/settings/providers`），不是参考库的声明式配置加载管线；变换域要有价值，必须先有「插件参与配置解析」的读取路径与消费场景，当前无任何消费者（第三方插件可经 hook/工具/事件满足全部现有扩展需求）。遵循本仓「不做投机基建」原则（同 `260921-opencode-v2能力对齐` 的 T-07 处置），记录为后续独立议题：若未来出现「插件需要改写 agent 定义/provider 路由」的真实需求，再按需求设计读取路径与重放语义
- [x] T-15 ✅: `permission.evaluate` 形态对齐（`agent`/`resources`/`metadata`/`source` 字段）——**明确不做（评审结论）**：本仓事件已含 `sessionID`/`toolName`/`permission`/`scope`/`decision`/`message`，并被 `docs/plugins/README.md` 作为平台自身契约文档化；参考库字段对齐仅在「跨平台移植参考库插件」场景有价值，而权限语义本身已分叉（参考库 allow/deny/ask vs 本仓 deny-only，D-2），逐字段对齐无法带来兼容性收益；字段按需扩展（出现消费者时再加）
- [x] T-16 ✅: P2 测试与回归——插件相关 **11 文件 / 64 测试全绿**（新增 5 文件 23 例 + 存量 6 文件 41 例）；gateway typecheck 0 error；ESLint 0 error；Prettier 通过

**P2 验收契约（已验证）**：
1. ✅ 示例插件注册 `plugin_echo` 工具 → 模型可见（`buildGatewayToolDefinitions` 包含）→ 沙箱执行端到端通过；权限落 `custom`（ask），yolo 下免审批路径验证通过。
2. ✅ 插件工具与内置工具同名被拒绝（保留名单注入生效）；重复注册被拒绝；非法名被拒绝。
3. ✅ 插件存储隔离：两插件同名 key 互不干扰；卸载后 `removeByPlugin` 清理。
4. ✅ 事件订阅在插件卸载后自动停止（eventSignal abort）；订阅先于首次迭代不丢事件。
5. ✅ `tool.transform` 的 add/remove 在下一回合生效（每回合构建，无缓存）；插件工具默认直连（未登记进折叠目录），渠道会话按既有 `filterEnabledGatewayToolsForSession` 默认隐藏。

### Phase 3: 发现、配置与热重载 —— 🔵 主体已交付（2026-09-24）
- [x] T-17 ✅: 声明式配置——`plugin/config.ts`：`<dataDir>/openawork.json` 的 `plugins` 数组（字符串 / `{package,options}` / `-target` 移除 / `*` 与 `prefix.*` 启用选择器）；解析错误按路径定位；配置文件缺失视为空配置；测试 5 例（语法/空值/错误定位/选择器匹配）
- [x] T-18 ✅: 目录发现 + 源解析——`plugin/source.ts`：`<dataDir>/plugins/*` 扫描（排序、跳过点目录）+ entrypoint 候选（`index.js/mjs/cjs`、`server.js/mjs`、`main.js`）+ spec 解析（file/directory/package 三态，含 watchPath）；**sidecar `import()` 验证通过**（`bun build --compile` 二进制运行时 `import('file:///...plugin.mjs')` 成功加载外部文件，spike 记录见 Notes）
- [x] T-19 ✅: 文件监听基础——`plugin/watcher.ts`：`fs.watch`（`persistent:false`）+ 200ms 防抖 + SHA-256 digest 去重（内容未变不回调）+ 监听器错误自愈 + `flushForTest` 测试钩子
- [x] T-20 ✅: 热重载——`plugin/loader.ts`：cache-bust `import()`（`?v=<ts>`，已实测 Node/Vitest 下均绕过模块缓存）+ 失败源保留追踪（修复文件自动重试）+ V1 id 幂等复用（卸载后同 id 重新激活）；测试 `plugin-loader.test.ts` 6 例（env/发现/移除/启用覆盖/缺失路径/热重载端到端）
- [x] T-21 ✅: supervisor——`plugin/supervisor.ts`（`guarded` 集合 + 加载器移除/停用豁免 + `GET /plugins` 投影）+ **enable/disable 运行时状态**：`plugin/state-store.ts`（`plugin_state` 表，按插件 id 持久化，查询对未迁移环境 fail-open）、加载器对已停用插件跳过激活（保留追踪、可热重载恢复）、管理 API `POST /plugins/:pluginId/disable|enable`（guarded 409、未知名 404）、`GET /plugins` 合并投影 `state: 'disabled'`、Web UI 行内「停用/启用」（含测试与 harness 断言）；失败降级（保留旧 revision 运行）与 pre/post 槽位未采用（P3 前缀 diff 简化为全量重建，见 T-06 偏差记录）
- [x] T-22 ✅: 状态诊断 API——`routes/plugins.ts`：`GET /plugins`（认证必需）投影 `{ id, source, state(active|failed+error), guarded }`；index.ts 注册；路由测试 2 例（401 + active/failed/guarded 投影）
- [x] T-23 ✅: P3 测试与回归——插件全集 **9 文件 / 47 测试全绿**（新增 config/loader/routes 共 13 例）；含 tool-definitions/tool-contract-parity/tool-folding/沙箱 hook 的扩展回归 **17 文件 / 102 测试全绿**；typecheck 0 error；ESLint 0 error

**P3 形态适配记录（与参考库的差异）**：
1. **仅全局级插件**：不做参考库的"项目级 `.opencode/plugins/`"——本仓网关是多用户单实例服务，无「当前项目」单入口，插件统一从 `<dataDir>`（全局）加载。
2. **仅监听 entrypoint 文件**：不做参考库的依赖图 digest（`createPluginSources` 的 files Map）；单文件插件布局下影响可忽略，依赖图监听列为后续项。
3. **启用选择器语义简化**：`["-*", "my-plugin"]` 中精确名同时匹配本地候选 basename 时视为"救援启用"（见 `loadPlugins` 两阶段装配）。

**P3 验收契约（已验证）**：
1. ✅ 修改本地插件文件 → flush 后 hook 逻辑切换为新内容（无需重启；测试断言 v1→v2 行为切换）。
2. ✅ 配置 `-my-plugin` 移除发现来源；`["-*", "keep-me"]` 保留指定插件、移除其余；guarded 插件豁免移除（单测覆盖）。
3. ✅ 失败源被追踪，缺失路径/导入失败仅记录 skipped 不中断启动。
4. ✅ `GET /plugins` 需认证；active/failed（含错误信息）+ guarded 投影正确。
5. 🟡 sidecar 二进制 `import()` 可行性已 spike 验证；**完整 sidecar e2e（编译网关二进制 + 加载插件）留 P4/P5 与安装面一并验证**。

### Phase 4: 安装与管理面 —— ✅ 已交付（2026-09-24）
- [x] T-24 ✅: 安装机制——`plugin/installer.ts`：**本地目录**（递归复制 + entrypoint 校验）与**单文件**（`<id>/index.<ext>`）安装；staging + rename 原子落位；`force` 覆盖；install id 消毒（`[A-Za-z0-9][A-Za-z0-9._-]{0,63}`）与 plugins 根目录约束；缺失源/无 entrypoint/不支持扩展名/源已在 plugins 内均被拒绝；测试 7 例。**npm / zip 明确不做**（网关无归档依赖；npm 安装需运行时包管理器，见 D-4；`bun add` 可行性验证降级为"import() 已验证、npm 安装留待专门评估"）
- [x] T-25 ✅: 管理 API——`routes/plugins.ts`：`GET /plugins`（含 `installId`、`guarded` 与 `disabled` 状态投影）、`POST /plugins/install`（Zod 校验 + 激活 + 返回 id/state）、`DELETE /plugins/:installId`（guarded 拒绝 409 → deactivate + **storage 清理** + 目录移除 + 重扫）、`POST /plugins/:installId/reload`（deactivate + 缓存破坏重激活）、`POST /plugins/:pluginId/disable|enable`（持久化启停 + 重扫）；未知名 404、缺失源 400；JWT 认证
- [x] T-26 ✅: web-client 封装——`packages/web-client/src/infra/plugins.ts`（`createPluginsClient`：list/install/remove/reload，统一 `HttpError` 与中文错误归一）+ `index.ts` 导出；web-client typecheck 通过
- [x] T-27 ✅: Web UI 插件管理面板——`plugin-registry.tsx` 新增 `third-party`（「已安装插件」）导航项 + `plugins-tab-content.tsx` 深链 `?plugin=third-party`；容器 `ThirdPartyPluginsPanel`（`useThirdPartyPlugins`：list/install/remove/reload/disable/enable + loading/error/empty 三态 + 操作状态消息）+ 展示层 `ThirdPartyPluginsView`（纯 props + 本地 UI 状态：清单行（状态点/徽章/来源/失败原因/已停用）、安装表单（React 19 `<form action>` + 覆盖开关）、重载/停用/启用/卸载（二次确认）、hover/focus 态、375px 响应式、E·Nebula token）；**真实 Chromium 三视口验收 64 断言全过**（`harness/third-party-plugins-3viewports`）；jsdom 测试：面板 8 例 + 外壳深链 1 例
- [x] T-28 ✅: P4 测试与验收——安装器 7 例 + 管理端点端到端 3 例（install→list→reload→uninstall 全链路 + 404/400 错误契约）；P4 范围回归 **17 文件 / 108 测试全绿**；typecheck 0 error、ESLint 0 error

**P4 验收契约（已验证）**：
1. ✅ 安装 → 列表 active → 重载 → 卸载后列表消失（API 端到端测试 + Web UI 三视口真实浏览器验收）；卸载同时清理插件存储；**UI 二次确认已交付**（T-27，harness 断言首次点击不触发回调）；**启停开关已交付**（T-21：`disable`/`enable` 持久化 + UI 行内按钮 + harness 断言停用行渲染与回调）。
2. ✅ 安装来源校验：缺失路径 400、无 entrypoint 400、不支持类型 400、install id 非法 400、guarded 409；卸载仅操作 plugins 根目录内的路径（目录穿越防护）；UI 顶部明确「与网关同进程、无沙箱，只安装信任代码」。
3. ✅ 管理 API 全部 JWT 认证 + Zod 边界校验；错误结构化（ApiError）。

### Phase 5: 内部插件化与生态 —— 🔵 部分交付（2026-09-24）
- [x] T-29 ✅: 内置工具组迁移——`plugin/builtin-groups.ts`：3 个可迁移工具组（`image-generation` / `desktop-control`（含 `computer_use`）/ `desktop-automation`）注册为 **guarded 内部插件**（`source: 'internal'`、`GET /plugins` 可见且 `guarded: true`、配置不可移除、注册幂等）；**门控链迁入插件平台**（`isBuiltinToolAllowedForUser` 按插件 id 组合判断、fail-closed），`filterPluginControlledToolsForUser` 从 `tools/plugin-tool-settings.ts` 移至插件模块，`stream.ts`/`stream-runtime.ts` 改引新模块；**存储（`user_settings.plugin_settings`）与 `/settings/plugins` UI 契约不变**——单用户部署下 per-user 设置即全局开关，逐请求读取保持"改完即生效"。`mcp`/`skills`/`websearch` 属资源导航面板（非开关语义），明确不迁移。验证：迁移前行为测试 9 例**原样通过**（行为不变）+ 新增 3 例（guarded 内部插件注册幂等 / fail-closed / 按组独立）
- [x] T-30 ✅: guarded 内部插件——`plugin/supervisor.ts`（`markPluginGuarded`/`isPluginGuarded`/`guardedPluginIds`）+ 加载器移除豁免 + `GET /plugins` 投影 `guarded`；内部组插件（T-29）是首个消费者，注册即打标
- [x] T-31 ✅: 示例插件 + 开发者文档——`docs/plugins/README.md`（快速开始 / 双 API / 来源与配置语法 / 热重载 / 能力域 / 安装管理 / 安全模型）+ 3 个示例（guard/tool/notify）；**示例有端到端测试**（`plugin-loader.test.ts` 加载 `guard-plugin.mjs` 并断言 `.env` 拦截生效）
- [x] T-32 ✅: V1→V2 迁移指南——`docs/plugins/migrate-v1.md`（入口形态 / 5 个 hook 映射表 / 新增能力 / 生命周期差异 / 配置迁移 / 校验清单）；参考库 `migrate-v1.mdx` 的本仓版本
- [x] T-33 ✅（定向）: 回归验证——插件域 **17 文件 / 108 测试全绿**；typecheck 0 error；ESLint 0 error；sidecar `import()` spike 通过。**全量 gateway 测试套件与桌面端 sidecar e2e 未运行**（工作树存在并行工作，全量结果噪声大；留待发布前收口）
- [x] T-34 ✅: 文档收口 + 记忆同步——workflow 全阶段状态更新、master_plan 同步、`index.md` 项目记忆写入（见「项目记忆」）

**P5 验收契约（已验证/调整）**：
1. ✅ 内置工具组开关行为与迁移前一致——迁移前 9 例行为测试原样通过；新增门槛链测试 3 例；`GET /plugins` 新增 3 个 guarded 内部插件条目。
2. ✅ 开发者可按 `docs/plugins/README.md` 完成"写插件 → 本地热重载调试 → 安装"全流程；示例有端到端测试佐证。
3. ✅ 旧插件兼容（shim）+ 迁移指南就绪；shim 与 v2 共存，废弃计划：待新格式生态稳定后另行评估（不设强制期限）。
4. 🟡 桌面端 sidecar 加载插件：`import()` 能力已 spike 验证；完整桌面 e2e 留待发布前收口。

## Notes

### 风险清单

| 风险 | 等级 | 缓解 |
|---|---|---|
| 宿主重构引入行为回归 | 🔴 高 | P1 验收契约 5 要求"零插件时逐字节一致"；接线点改动 ≤3 文件分段 |
| sidecar 二进制内 `import()`/`bun add` 不可用 | 🔴 高 | P3/P4 前置可行性验证（T-18/T-24 首步即验证）；不可行则退回 HTTP 安装 + 目录加载 |
| `tool.transform` 与工具折叠/权限链路冲突 | 🟡 中 | T-12 强制走 6 处登记 + guard 测试；折叠目录含插件工具 |
| 热重载引发开发期状态抖动 | 🟡 中 | digest 指纹去重 + 防抖；前缀 diff 只重建变更插件 |
| Effect 双 API 复杂度 | 🟡 中 | Promise 层是薄适配器；Effect 层类型独立测试 |
| 与既有"AI 自助管理四域"工作冲突 | 🟢 低 | 该批已收口；插件管理工具（如未来 `plugin_manage`）不在本方案范围 |

### 明确不做（本方案边界）

- **TUI 插件**：本仓无 TUI 形态，不移植。
- **RPC 跨进程插件**：无消费场景（D-7），列为 P5 后评估。
- **`shell.create.before` / `aisdk.*` / `session.http.*` / `ws.*` hook**：按需后补，首批不承诺。
- **`session.compaction` / `title` / `generate` 跳过模型调用的 hook**：涉及会话核心链路，独立评估后再做。
- **Provider 裸 API Key / 渠道凭据 / 权限规则放宽**：延续既有安全边界，永不做。

### 与既有工作的关系

- 本方案**不替代** `260921-opencode-v2能力对齐`（工具维度对齐）与 `260924-AI自助管理扩展四域`（管理工具），是独立第三条线（插件架构维度）。
- Web UI 插件面板（T-27）与 `260924-插件技能MCP管理界面重构`（视觉/信息架构重构）在 `plugins-tab-content.tsx` 上有交集：先完成该重构，T-27 在其数据驱动外壳上扩展，避免二次返工。
- 记忆同步点：P1 完成时记录宿主架构决策；P3 完成时记录热重载/发现机制；P5 完成时记录内部插件化约定。
