# .agentdocs/workflow/260329-子代理工具提示词完全对齐方案.md

## 任务概览

对照 `temp/opencode` 与 `temp/oh-my-openagent`，审查并收敛 OpenAWork 当前仓库中的子代理功能、基础工具定义与提示词/系统提示词，使使用方式、源定义与关键约束尽可能做到完全一致。

## 当前分析

- `.agentdocs/index.md` 已记录多轮针对 `task` 工具、内置工具命名、agent prompt 快照与子代理运行时的对标工作，说明当前仓库不是缺少全部能力，而是存在“部分对齐、部分漂移、部分仍为 reference-only”的混合状态。
- 用户本次要求从“是否完全一致”出发，因此不仅要核对 API/Schema 名称，还要核对子代理实际运行语义、工具 source-of-truth、prompt 文案与注入方式是否一致。
- 本轮重点是冻结三类事实源：子代理执行链路、工具定义链路、prompt/指令链路；随后以差异为准直接修正仓库实现。

## Complexity Assessment

- Atomic steps: 5+ → +2
- Parallel streams: 当前仓库 / temp/opencode / temp/oh-my-openagent 可并行审计 → +2
- Modules/systems/services: agent-gateway + web + shared + temp references + .agentdocs → +1
- Long step (>5 min): yes → +1
- Persisted review artifacts: yes → +1
- OpenCode available: yes → -1
- **Total score**: 6
- **Chosen mode**: Full orchestration
- **Routing rationale**: 这是一次跨当前实现、两个参考仓库与既有冻结决策的多维收口，必须保留显式工作流与验证痕迹，避免只对齐名称而遗漏运行时语义或 prompt 注入方式。

## Implementation Plan

### Phase 1：三方事实源定位
- [ ] T-01：定位当前仓库的子代理、基础工具、提示词事实源
- [ ] T-02：定位 `temp/opencode` 的对应事实源
- [ ] T-03：定位 `temp/oh-my-openagent` 的对应事实源

### Phase 2：差异冻结与实现对齐
- [ ] T-04：输出三方差异清单，按“运行时语义 / 工具定义 / prompt 文案”分类
- [ ] T-05：在当前仓库中完成差异修正

### Phase 3：验证与收口
- [ ] T-06：运行 diagnostics / 定向测试 / 类型检查或构建验证
- [ ] T-07：回写工作流结论与必要记忆

## Notes

- 本工作流创建于 2026-03-29，用于本轮“要求与两个参考仓库完全一致”的收口任务。
- 允许复用既有对标工作流中的结论，但必须再次落到当前代码事实上验证。
