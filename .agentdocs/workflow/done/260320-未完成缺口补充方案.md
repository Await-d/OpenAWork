# .agentdocs/workflow/260320-未完成缺口补充方案.md

## 任务概览

基于 2026-03-20 深度代码检查，整理所有「方案声称完成但代码实际未接线/缺失/有缺陷」的功能缺口。
本方案覆盖 5 个类别共 **20 个未完成项**，按优先级排序。

> 状态说明：🟡 待开始 | 🔵 进行中 | ✅ 完成 | ❌失败

---

## Part 1：Gateway 集成缺口（高优先级）

### Phase GW（W1）

- [x] GW-01 ✅：新建 `services/agent-gateway/src/github/router.ts`（Fastify 插件，`POST /github/webhook`），调用 `GitHubTriggerImpl.handleWebhook()`，在 `index.ts` 注册 `githubRoutes` — GitHub webhook 可被外部触发
- [x] GW-02 ✅：修改 `channels/router.ts` 中 `notify` 回调，接入 `AutoReplyPipeline.handle()`，注入 `onAgentRun` 触发 agent session — channel 收消息自动触发 agent
- [x] GW-03 ✅：替换 `cron/router.ts` 中 `defaultHandler`（当前为空函数），改为调用 gateway agent session 执行 job.prompt — 定时任务真实执行
- [x] GW-04 ✅：在 `channels/router.ts` 补充注册 feishu/dingtalk factory（`OC-16/OC-17` 已设计实现，只需 registerFactory） — feishu/dingtalk channel 可正常 start

**验收标准**
- GitHub App 推送 PR 事件后，gateway 能接收并触发 agent session
- Telegram bot 收到消息后，AutoReplyPipeline 自动回复（含 streaming）
- Cron job 触发后，agent 根据 prompt 执行并记录历史
- feishu/dingtalk channel 创建后可正常 start（不抛 factory 未找到错误）

---

## Part 2：shared-ui 缺失组件（中优先级）

### Phase UI（W2）

- [x] UI-01 ✅：新建 `packages/shared-ui/src/DeveloperModePanel.tsx` — 时间轴列表 + 类型过滤 + payload 详情面板，shared-ui tsc EXIT:0
- [x] UI-02 ✅：新建 `packages/shared-ui/src/SSHConnectionPanel.tsx` — 连接列表/状态指示/添加表单/Bind 会话按钮
- [x] UI-03 ✅：新建 `packages/shared-ui/src/AgentVizPanel.tsx` — 时间轴视图含连接线/图标/耗时/错误展示
- [x] UI-04 ✅：新建 `packages/shared-ui/src/PairingPanel.tsx` — Host/Client 模式切换，Host 侧 QRCodeDisplay + 已配对设备列表，Client 侧 QRCodeScanner + 手动输入

**验收标准**
- 四个组件在 shared-ui/src/index.ts 正确导出
- tsc --noEmit 通过

---

## Part 3：Mobile 端核心页面缺失（中优先级）

### Phase MOB（W2-W3）

- [x] MOB-01 ✅：新建 `apps/mobile/src/screens/ChatScreen.tsx` — WebSocket 流式对话 + 消息列表 + 输入框 + loading 状态
- [x] MOB-02 ✅：新建 `apps/mobile/src/screens/SessionsScreen.tsx` — 离线优先列表 + 远端同步 + 新建会话
- [x] MOB-03 ✅：新建 `apps/mobile/src/screens/SettingsScreen.tsx` — Gateway URL / Provider 选择 / MCP Server CRUD / 登出
- [x] MOB-04 ✅：新建 `apps/mobile/src/navigation/AppNavigator.tsx` — 状态路由（loading→onboarding/sessions/chat/settings），Onboarding 检测

**验收标准**
- Mobile 端可独立路由到 Chat/Sessions/Settings 三个页面
- Onboarding 完成后自动跳转到 Sessions 页

---

## Part 4：实现缺陷/风险项（需修复）

### Phase FIX（W1-W2）

- [x] DEF-01 ✅：`packages/web-client/src/auth.ts` 中 `GATEWAY = ''` 空常量 — 改为 `configureGateway(url)` + `getGatewayUrl()` 导出，调用方显式注入，web-client tsc EXIT:0
- [x] DEF-02 ✅：`packages/web-client/src/gateway-sse.ts` 中 `onerror` 回调被重复赋值 — 删除重复赋值，合并为单一处理逻辑
- [x] DEF-03 ✅：`apps/desktop/src/store/secure-storage.ts` 使用明文 JSON — 改用 Web Crypto AES-GCM（256-bit，随机IV）加密后再写入 plugin-store，desktop tsc EXIT:0
- [x] DEF-04 ✅：`apps/desktop/src/notification/notification-manager.ts` `send()` 补充 `emit('notification-action', action)` 调用，UI 侧可监听 Tauri 事件实现跳转
- [x] DEF-05 ✅：`apps/mobile/app/_layout.tsx` 第68行 `Stack` JSX 类型错误（@types/react@19 + expo-router 版本冲突）— 升级 mobile 到 @types/react@19.2.14 + skipLibCheck + worker/index.ts 类型修复，mobile tsc EXIT:0

**验收标准**
- web-client auth 可正确打到 gateway 地址
- SSE onerror 不丢失连接错误事件
- desktop 密钥存储经过加密
- 点击通知可跳转对应会话/频道
- mobile app _layout.tsx tsc 通过

---

## Part 5：Agent-core 实现简化项（低优先级，可选优化）

- [x] OPT-01 ✅：`ralph-loop/index.ts` DONE 检测改为行首匹配（`line.trimStart().startsWith(doneKeyword)`），防止自然语言中出现 DONE 字样误触发
- [x] OPT-02 ✅：`routing.ts` 意图路由为 regex 启发式 — 当前方案可用，升级为 LLM 判别属于架构演进，暂保留现状（已记录为未来演进方向）
- [x] OPT-03 ✅：`context/compact.ts` token 计数改为 unicode 感知分词估算（CJK字符×2×1.3，英文词片×1.3），比 `length/4` 更精确

---

## Part 6：已修复项（本次已完成）

- [x] FIX-01 ✅：`packages/agent-core/src/index.ts` 补充导出 `SSHConnectionManagerImpl` + SSH 类型
- [x] FIX-02 ✅：`services/agent-gateway/src/tool-sandbox.ts` 注册 `webSearchTool` 到 ToolSandbox 并加入白名单
- [x] FIX-03 ✅：新建 `apps/desktop/src/hooks/useSSHConnection.ts` — desktop 端 SSH React hook

---

## 未完成项统计

| 类别 | 数量 | 优先级 |
|------|------|--------|
| ~~Gateway 集成缺口~~ | ~~4项（GW-01~04）~~ ✅ 已全部完成 | 高 |
| ~~shared-ui 缺失组件~~ | ~~4项（UI-01~04）~~ ✅ 已全部完成 | 中 |
| ~~Mobile 核心页面缺失~~ | ~~4项（MOB-01~04）~~ ✅ 已全部完成 | 中 |
| ~~实现缺陷/风险项~~ | ~~5项（DEF-01~05）~~ ✅ 已全部完成 | 中 |
| ~~实现简化可选优化~~ | ~~3项（OPT-01~03）~~ ✅ 已全部完成 | 低 |
| **合计** | **20项** | — |

---

## 依赖关系

```
GW-01（github router）独立
GW-02（autoReply）依赖 channels/router 现有结构
GW-03（cron handler）依赖 gateway agent session 接口
GW-04（feishu/dingtalk factory）依赖已有 channels/feishu.ts、channels/dingtalk.ts

UI-01~UI-04 独立
MOB-01~MOB-03 依赖 MOB-04（导航）
MOB-04 依赖 apps/mobile 现有 hooks/db/store

DEF-01 独立（web-client 配置注入）
DEF-02 独立（SSE 实现修复）
DEF-03 独立（桌面安全存储升级）
DEF-04 依赖 DEF-03 和 Tauri notification plugin
DEF-05 独立（类型版本冲突修复）
```

---

## 里程碑

- MGW（W1）：Gateway 4 项集成缺口全部接线，外部触发链路可用
- MFIX（W1）：5 项实现缺陷修复完成，安全存储/auth/通知可用
- MUI（W2）：4 个缺失 shared-ui 组件完成并导出
- MMOB（W3）：Mobile 端 4 个核心页面完成，可独立运行

---

## 备注

- 本方案为独立补充方案，不修改任何已完成方案的任务标记
- GW-02/GW-03 需要确认 gateway agent session 执行接口（`/sessions/:id` + stream）的具体调用方式后实现
- Mobile 端页面可先实现骨架，复用 shared-ui 组件降低重复开发
- OPT 项目不影响功能正确性，可在稳定后作为质量提升专项处理
- skill-types 包为纯协议类型包，无需补充 runtime 实现
