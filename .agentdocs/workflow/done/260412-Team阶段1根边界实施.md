# Team 阶段1根边界实施

## Task Overview

在已冻结的 Team A-E 方案基础上，开始真正的代码实施。当前阶段只做 **Stage 1：TeamWorkspace 根边界 + workspace-first 路由/contract**，目标是把 Team 从“以 shared session 反推的协作页”推进到“以 TeamWorkspace 为根锚点的最小可运行实现”。

## Current Analysis

- 旧的 `260410-团队页统一开发主计划` 与 `260412-Team详细任务实施方案` 已完成冻结并归档。
- 用户已明确纠偏：不是只创建方案，而是创建方案后继续完成实际工作。
- 当前代码里与 Stage 1 最相关的区域横跨：
  - `services/agent-gateway/src/routes/team.ts`
  - `packages/web-client/src/team.ts`
  - `apps/web/src/App.tsx`
  - `apps/web/src/pages/TeamPage.tsx`
  - `apps/web/src/pages/team/use-team-collaboration.ts`
  - `apps/web/src/pages/team/runtime/team-runtime-reference-data.tsx`
- 当前工作区存在若干与本次 Team 实施无关的用户/既有改动，实施中必须显式避开。

## Solution Design

- 先实现最小闭环：
  1. Gateway 增加 TeamWorkspace 根级 list/detail/create/update 能力
  2. web-client 增加 TeamWorkspace contract 与 client 方法
  3. Web 支持 `/team/:teamWorkspaceId` 最小路由锚点
  4. TeamPage 进入 workspace-first 容器，但暂不改写全部子 tab
- 对旧 Team runtime 参考页保持最小侵入：先让它接收 workspace 级上下文，再逐阶段替换 shared-session 主链。

## Complexity Assessment

- Atomic steps: 5+（gateway / web-client / web 路由 / state 容器 / 验证 / git） → +2
- Parallel streams: yes（gateway、client、frontend 切口探索可并行） → +2
- Modules/systems/services: 3+（gateway / web-client / web） → +1
- Long step (>5 min): yes → +1
- Persisted review artifacts: yes → +1
- OpenCode available: yes → -1
- **Total score**: 6
- **Chosen mode**: Full orchestration
- **Routing rationale**: 这是从冻结方案切入真实实现的第一刀，涉及多模块联动与显式阶段控制，必须保留工作流与运行态总表。

## Implementation Plan

### Phase 1：切口确认
- [x] T-01：确认 gateway / web-client / web 的最小实施切口 ✅
- [x] T-02：冻结 Stage 1 的提交边界与验证命令 ✅

### Phase 2：根 contract 实现
- [x] T-03：实现 TeamWorkspace 根接口与最小持久化/返回结构 ✅
- [x] T-04：实现 TeamWorkspace web-client contract ✅

### Phase 3：workspace-first 入口
- [x] T-05：实现 `/team/:teamWorkspaceId` 路由锚点与 TeamPage 最小 workspace-first 容器 ✅

### Phase 4：验证与提交
- [x] T-06：完成 Stage 1 验证并创建原子提交（按 gateway / web-client / web / docs 分拆）✅

## Notes

- 本工作流只覆盖 Stage 1 实施，不把后续读模型切换、bridge 显式化、页面热点改造混进来。
- 运行态总表位于 `.agentdocs/runtime/260412-Team阶段1根边界实施/master_plan.md`。
- 已完成文件：
  - `services/agent-gateway/src/db.ts`
  - `services/agent-gateway/src/routes/team.ts`
  - `services/agent-gateway/src/__tests__/team-collaboration.test.ts`
  - `packages/web-client/src/team.ts`
  - `packages/web-client/src/index.ts`
  - `packages/web-client/src/__tests__/team-client.test.ts`
  - `apps/web/src/App.tsx`
  - `apps/web/src/App.test.tsx`
  - `apps/web/src/pages/TeamPage.tsx`
  - `apps/web/src/pages/team/use-team-workspace-state.ts`
  - `apps/web/src/pages/team/runtime/team-runtime-reference-data.tsx`
  - `apps/web/src/pages/TeamPage.test.tsx`
  - `apps/web/src/pages/TeamPage.interaction.test.tsx`
- 已完成验证：
  - `pnpm --filter @openAwork/agent-gateway exec vitest run src/__tests__/team-collaboration.test.ts`
  - `pnpm --filter @openAwork/web-client exec vitest run src/__tests__/team-client.test.ts`
  - `pnpm --filter @openAwork/web exec vitest run src/pages/TeamPage.test.tsx src/pages/TeamPage.interaction.test.tsx src/App.test.tsx`
  - `pnpm typecheck`
  - `pnpm --filter @openAwork/agent-gateway build`
  - `pnpm --filter @openAwork/web build`
- Stage 1 提交边界：
  - `feat(gateway): ...`
  - `feat(web-client): ...`
  - `feat(web): ...`
  - `docs(agentdocs): ...`
