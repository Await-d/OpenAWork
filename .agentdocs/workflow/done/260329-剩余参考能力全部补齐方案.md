# .agentdocs/workflow/260329-剩余参考能力全部补齐方案.md

## 任务概览

在前两轮对齐审计和关键偏差修正的基础上，继续把 OpenAWork 当前仓库中相对 `temp/opencode` 与 `temp/oh-my-openagent` 仍未实现或仅部分实现的 reference 能力全部补齐，覆盖工具、特殊功能与子代理模型支持。

## 当前分析

- 目前已完成 `task/background_output/background_cancel`、capabilities 真值、`websearch` enabled-tools、`workspace_review_diff` 读类归类、以及子代理模型最小真相源的收口。
- 剩余缺口已经从“使用方式不一致”收敛成更明确的“真实未实现能力族”：包括 `session_list/read/search/info`、`ast_grep_search/replace`、`call_omo_agent`、`skill_mcp`、`interactive_bash`、`look_at` 等。
- 除工具外，还需要把子代理模型/agent 配置面继续提升，尽量接近参考仓库中的 agent/category override、fallback candidate 与 metadata 使用方式，而不是只停留在 prompt 对齐。

## Complexity Assessment

- Atomic steps: 5+ → +2
- Parallel streams: 工具补齐 / 特殊功能补齐 / 子代理模型补齐可并行推进 → +2
- Modules/systems/services: agent-gateway + shared + agent-core + web + temp references + .agentdocs → +1
- Long step (>5 min): yes → +1
- Persisted review artifacts: yes → +1
- OpenCode available: yes → -1
- **Total score**: 6
- **Chosen mode**: Full orchestration
- **Routing rationale**: 这已经不是单点修补，而是要把剩余参考能力整批落入当前仓库的 runtime/capabilities/test 体系，必须显式跟踪分阶段推进与验证闭环。

## Implementation Plan

### Phase 1：当前实现与落点冻结
- [x] T-01：确定每个剩余 reference tool family 在当前仓库中的承载落点
- [x] T-02：确定哪些能力可基于现有 routes/db/runtime 直接包装实现，哪些需要新增执行器

### Phase 2：工具与特殊功能补齐
- [x] T-03：补齐 `session_list/read/search/info`
- [x] T-04：补齐 `ast_grep_search/replace`
- [x] T-05：补齐 `skill_mcp`、`look_at`
- [x] T-06：补齐 `interactive_bash`、`call_omo_agent`

### Phase 3：子代理模型与目录补齐
- [x] T-07：扩展 managed agent / delegated task 的模型配置与元数据
- [x] T-08：同步 capabilities 与 agent routes 的暴露面

### Phase 4：验证与收口
- [x] T-09：更新单测与 verification 脚本
- [x] T-10：运行 diagnostics、build、test 与 Oracle 终局复核

## Notes

- 本工作流创建于 2026-03-29，用于“全部补齐剩余参考能力”的实现阶段。
- 本轮完成标准不是“说明有哪些缺口”，而是把这些缺口实际落到当前 runtime 中，并让 capability 目录与测试证据同步收口。
- 已完成的关键收口：
  - gateway runtime 新增 `session_list/session_read/session_search/session_info`、`ast_grep_search/ast_grep_replace`、`skill_mcp`、`interactive_bash`、`call_omo_agent`、`look_at`。
  - `packages/mcp-client` 与 `packages/skill-types` 已扩展 resource/prompt 读取能力，供 `skill_mcp` 复用。
  - Agents 页面已增加“默认模型路由”编辑区，可编辑并提交 `model / variant / fallbackModels`。
  - builtin agent 默认模型会从 reference 候选回灌，managed override 会被 runtime 真正消费；`task` category 路由与 `look_at` 优先读取 managed 配置，再回退 reference。
  - 验证已通过：`@openAwork/skill-types` / `@openAwork/shared` / `@openAwork/mcp-client` / `@openAwork/agent-gateway` build、`@openAwork/web` build、`@openAwork/agent-gateway` 完整 test、Agents 页面定向测试、Oracle 终局复核。
- Memory sync: completed
