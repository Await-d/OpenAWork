# 260321 优先级调整与修复方案

## 任务概述

基于代码层面实际检查结果，识别出当前代码库中真正未完成/需修复的问题，
建立优先级清单，并标明每个任务对应的原方案文件。

## Complexity Assessment

- 原子步骤：5+ → +2
- 并行流：可并行 → +2
- 涉及模块：gateway + web + agent-core → +1
- 单步 >5min → +1
- 持久化产物 → +1
- OpenCode 可用 → -1
- **Total: +6 → Full orchestration**
- **Routing rationale**: 跨 gateway/web/agent-core 三层修复，含 DB schema + 路由实现 + 前端接线，必须全流程协调。

---

## 实际问题清单（代码检查确认）

### P0：功能性骨架（调用会返回空数据）

这些端点已存在但永远返回空数组，前端虽已接线但无实际数据。

| 编号 | 问题 | 位置 | 原方案 |
|---|---|---|---|
| FIX-01 | `GET /settings/mcp-status` 返回 `{ servers: [] }` 硬编码空数组 | `services/agent-gateway/src/routes/settings.ts:22` | `260321-mock数据真实化方案.md` GW-06 |
| FIX-02 | `GET /settings/diagnostics` 返回 `{ diagnostics: [] }` 硬编码空数组 | `services/agent-gateway/src/routes/settings.ts:55` | `260321-mock数据真实化方案.md` GW-06 |
| FIX-03 | `GET /settings/workers` 返回 `{ workers: [] }` 硬编码空数组 | `services/agent-gateway/src/routes/settings.ts:63` | `260321-mock数据真实化方案.md` GW-06 |

### P1：前端硬编码问题

| 编号 | 问题 | 位置 | 原方案 |
|---|---|---|---|
| FIX-04 | `SettingsPage` Provider 列表使用 `DEFAULT_PROVIDERS` 硬编码，未从 Gateway 动态加载 | `apps/web/src/pages/SettingsPage.tsx:71-103` | `260321-mock数据真实化方案.md` FE-06 |
| FIX-05 | `SettingsPage` `BudgetAlert` 传入硬编码 `currentCostUsd={5.33}` | `apps/web/src/pages/SettingsPage.tsx:476` | `260321-mock数据真实化方案.md` FE-06 |

### P2：workspace 功能前端接线不完整

| 编号 | 问题 | 位置 | 原方案 |
|---|---|---|---|
| FIX-06 | `ChatPage` 中 `useWorkspace` hook 存在但需确认是否真正调用 PATCH `/sessions/:id/workspace` 和 GET `/workspace/tree` | `apps/web/src/pages/ChatPage.tsx` + `apps/web/src/hooks/useWorkspace.ts` | `260321-workspace-context-feature.md` T-10/T-11 |
| FIX-07 | `Layout.tsx` 右侧面板 `FileTreePanel` / `FileSearch` / `ContextPanel` 是否真正连接了 workspace API（还是传空数组） | `apps/web/src/components/Layout.tsx` | `260321-workspace-context-feature.md` T-10/T-11/T-12 |

### P3：结构问题（不影响运行时但影响代码规范）

| 编号 | 问题 | 位置 | 原方案 |
|---|---|---|---|
| FIX-08 | `packages/agent-core/src/permissions/index.ts` 缺失（有 `permission-manager.ts` 但无 barrel） | `packages/agent-core/src/permissions/` | `260319-缺陷补充与方案整合.md` P-08 |

---

## 实施计划

### Phase 1：Gateway 骨架端点补全（P0）

- [x] FIX-01 ✅：`/settings/mcp-status` 已从 `user_settings` 表读取 `mcp_servers` 配置，返回结构化状态列表
  → 对应 `260321-mock数据真实化方案.md` **GW-06**
- [x] FIX-02 ✅：`/settings/diagnostics` 已从 `audit_logs` 表聚合 is_error=1 的工具调用错误作为诊断信息
  → 对应 `260321-mock数据真实化方案.md` **GW-06**
- [x] FIX-03 ✅：`/settings/workers` 已从 `user_settings` 表读取 `workers` key，返回已注册 worker 列表
  → 对应 `260321-mock数据真实化方案.md` **GW-06**

### Phase 2：前端硬编码修复（P1）

- [x] FIX-04 ✅：`SettingsPage` Provider/ActiveSelection/MCP 配置已通过 Gateway `GET/PUT /settings/providers`、`GET/PUT /settings/mcp-servers` 实现真实持久化，移除所有 localStorage 缓存，改为初始化时从网关拉取
  → 对应 `260321-mock数据真实化方案.md` **FE-06**
- [x] FIX-05 ✅：`BudgetAlert.currentCostUsd` 已改为使用 `monthlyCostUsd` state（来自 `/usage/breakdown` API），`budgetUsd` 使用 `usageBudget` — SettingsPage.tsx:535
  → 对应 `260321-mock数据真实化方案.md` **FE-06**

### Phase 3：workspace 功能前端接线核查与补全（P2）

- [x] FIX-06 ✅：`useWorkspace.ts` hook 已完整实现（setWorkspace/clearWorkspace/validatePath/fetchTree/fetchFile 全部调用真实 API）
  → 对应 `260321-workspace-context-feature.md` **T-09/T-10** — **代码检查已确认完成**
- [x] FIX-07a ✅：`Layout.tsx` `FileTreePanel` 已连接真实 `treeNodes`（来自 `workspace.fetchTree`）；`WorkspaceSelector` 已连接 `workspace.setWorkspace`；`ContextPanel` 已根据 `workingDirectory` 动态渲染
  → 对应 `260321-workspace-context-feature.md` **T-10/T-12** — **代码检查已确认完成**
- [x] FIX-07b ✅：`Layout.tsx` `FileSearch.onSearch` 已接入真实搜索；新增 Gateway `GET /workspace/search` 端点 + `useWorkspace.searchFiles()` hook
  → 对应 `260321-workspace-context-feature.md` **T-11**

### Phase 4：结构修复（P3）

- [x] FIX-08 ✅：`packages/agent-core/src/permissions/index.ts` 已创建，导出 `PermissionScope`/`PermissionRecord`/`PermissionManagerInterface`/`PermissionManager`，LSP 0 错误
  → 对应 `260319-缺陷补充与方案整合.md` **P-08**

---

## 方案文件对照表

| 修复编号 | 原方案文件 | 原任务 ID |
|---|---|---|
| FIX-01 | `260321-mock数据真实化方案.md` | GW-06 |
| FIX-02 | `260321-mock数据真实化方案.md` | GW-06 |
| FIX-03 | `260321-mock数据真实化方案.md` | GW-06 |
| FIX-04 | `260321-mock数据真实化方案.md` | FE-06 |
| FIX-05 | `260321-mock数据真实化方案.md` | FE-06 |
| FIX-06 | `260321-workspace-context-feature.md` | T-09/T-10 |
| FIX-07 | `260321-workspace-context-feature.md` | T-10/T-11/T-12 |
| FIX-08 | `260319-缺陷补充与方案整合.md` | P-08 |

---

## 已确认完成的任务（无需修复）

以下在代码检查中验证为真实实现，原方案中对应任务可标记完成：

- `260321-mock数据真实化方案.md` **GW-01**：`GET /usage/records` + `GET /usage/breakdown` ✅ 已实现
- `260321-mock数据真实化方案.md` **GW-02**：`/team/*` CRUD ✅ 已实现  
- `260321-mock数据真实化方案.md` **GW-03**：channels CRUD（接入 ChannelManager）✅ 已实现
- `260321-mock数据真实化方案.md` **GW-04**：`/cron/jobs` CRUD ✅ 已实现
- `260321-mock数据真实化方案.md` **GW-05**：`/workflows/templates` ✅ 已实现
- `260321-mock数据真实化方案.md` **DB-01**：DB schema（usage_records/team_*/workflow_templates）✅ 已实现
- `260321-mock数据真实化方案.md` **FE-01**：`UsagePage` 接线 ✅ 已实现
- `260321-mock数据真实化方案.md` **FE-02**：`TeamPage` 接线 ✅ 已实现
- `260321-mock数据真实化方案.md` **FE-03**：`ChannelsPage` 接线 ✅ 已实现
- `260321-mock数据真实化方案.md` **FE-04**：`SchedulesPage` 接线 ✅ 已实现
- `260321-mock数据真实化方案.md` **FE-05**：`WorkflowsPage` 接线 ✅ 已实现
- `260321-workspace-context-feature.md` **T-02**：`POST /sessions` 支持 workingDirectory ✅ 已实现
- `260321-workspace-context-feature.md` **T-03**：`PATCH /sessions/:id/workspace` ✅ 已实现
- `260321-workspace-context-feature.md` **T-04**：`GET /workspace/tree` ✅ 已实现
- `260321-workspace-context-feature.md` **T-05**：`GET /workspace/file` ✅ 已实现
- `260321-workspace-context-feature.md` **T-06**：stream.ts 注入工作区上下文 ✅ 已实现（buildWorkspaceContext 函数）

---

## Notes

- 代码检查日期：2026-03-21
- mock 数据真实化方案中描述的大部分工作**已实际完成**，仅剩少量骨架端点和硬编码问题
- workspace-context 的 Gateway 层**已全部实现**，待核查的是前端接线部分
- `permissions/index.ts` 缺失是结构问题，不影响运行（根 index.ts 走的是 `./permission/index.js`）
