# 竞品差异化功能方案——让用户"用了回不去"

## Task Overview
基于对 OpenAWork 全代码库的深度审查，以 Cursor / Claude Desktop / ChatGPT / Windsurf / Devin 重度用户视角，为 12 个关键功能改进项（P0×3 + P1×4 + P2×5）创建详细的技术方案与设计文档。

## Complexity Assessment
- 原子步骤：12 个独立功能方案 → +2
- 并行流：12 方案互相独立，可全部并行 → +2
- 模块/系统/服务：agent-core, shared-ui, multi-agent, artifacts, web, gateway 6+ → +1
- 单步 >5min：每个方案需要深度分析现有代码与架构适配 → +1
- 持久化审查产物：需写入 .agentdocs/ → +1
- OpenCode 可用：→ -1
- **总分：6**
- **选定模式：Full orchestration**
- **路由理由：12 个独立方案可高度并行，且需要持久化供后续实施参考**

## Implementation Plan

### Phase 1: P0 — 核心迁移阻力（缺了就不会来）
- [x] T-01: 跨会话记忆系统（Memory） ✅
- [x] T-02: Artifact 实时预览 + 可编辑产物系统 ✅
- [x] T-03: 变更预览 + Apply/Reject 交互系统 ✅

### Phase 2: P1 — 留存钩子（有了就不想走）
- [x] T-04: Agent 运行实时可视化 Timeline ✅
- [x] T-05: 上下文窗口可视化管理 ✅
- [x] T-06: 全文搜索 + 智能会话检索 ✅
- [x] T-07: 消息质量反馈 + 自适应 ✅

### Phase 3: P2 — 竞争壁垒（护城河）
- [x] T-08: Workflow 可视化编辑器 ✅
- [x] T-09: 项目级 Agent 配置（Agent Profiles） ✅
- [x] T-10: 智能错误恢复 + 断点续跑 ✅
- [x] T-11: 智能通知 + 异步结果推送 ✅
- [x] T-12: 协作模式（团队共享） ✅

## 方案索引

| ID | 方案 | 文件 | 详细程度 |
|----|------|------|----------|
| T-01 | 跨会话记忆系统 | `results/T-01-跨会话记忆系统.md` | 完整代码实现（~1200行） |
| T-02 | Artifact 实时预览 | `results/T-02-artifact实时预览.md` | 完整代码实现（~1500行） |
| T-03 | 变更预览 + Apply/Reject | `results/T-03-变更预览-apply-reject.md` | 完整架构+API+组件（~800行） |
| T-04 | Agent Timeline 可视化 | `results/T-04-agent-timeline可视化.md` | 完整架构+事件模型+组件 |
| T-05+06 | 上下文管理+搜索 | `results/T-05-06-上下文管理-搜索.md` | 完整数据模型+FTS5+组件 |
| T-07+09 | 反馈+Agent配置 | `results/T-07-09-反馈-agent配置.md` | 完整DDL+Zod+前端wireframe（~800行） |
| T-08 | Workflow 可视化编辑器 | `results/T-08-workflow可视化编辑器.md` | React Flow架构+节点体系 |
| T-10+11+12 | 恢复+通知+协作 | `results/T-10-11-12-恢复-通知-协作.md` | Checkpoint+通知+团队模型 |

## Notes
- 每个方案独立成文，写入 `.agentdocs/runtime/260404-竞品差异化功能方案/results/`
- P0-1 和 P0-2 方案包含完整可实施的 TypeScript 代码（SQLite DDL、Zod schema、Store 实现、前端组件）
- P0-3 到 P2-12 方案包含完整的架构设计、数据模型、API 设计、前端组件设计、实施阶段和验证矩阵
- 所有方案均基于对现有代码库的深度分析，确保与现有架构模式一致（better-sqlite3 同步API、Zod 校验、Zustand store、Fastify routes）
- 2026-04-04 补充实施进展：T-03 已按 MVP 收口为会话级变更审阅闭环（`SessionsPage` 详情页中的文件改动审阅、恢复预览、应用恢复、工作区还原），并已通过 `SessionsPage.test.tsx` 自动化验证与真实浏览器页面检查。
- 2026-04-05 补充实施进展：T-04 已按 MVP 收口为 Agent Timeline 可视化（`session_run_events` + ChatPage 右侧 `viz` tab 中的 `AgentDAGGraph` / `AgentVizPanel`），并已补齐 `ChatPage.test.tsx` 的 run-events 回归与真实浏览器页面验证。
- 2026-04-05 补充实施进展：T-05 已按 MVP 收口为上下文窗口可视化管理（聊天顶部 `ContextUsageMeter`、右侧概览中的 `ContextPanel`、显式“立即压缩会话”入口），并已通过相关单测、Web 构建与真实浏览器压缩动作验证。
- 2026-04-05 补充实施进展：T-07 已接通 Phase 1 MVP（assistant 消息 👍/👎、gateway `message_ratings` 持久化、会话级 rating 列表/写入/删除 API），并已通过 store 单测、前端定向回归、构建与真实浏览器点赞回显验证。
- 2026-04-05 补充实施进展：T-09 已扩展到 Phase 1 MVP（聊天顶部 `toolSurfaceProfile` quick switch、设置页默认工具配置档、`useSessions.newSession()` 默认继承、手动 agent 选择写回 session metadata），并已通过 `ChatPage.test.tsx`、`connection-tab-content.test.tsx`、`useSessions.test.tsx`、Web 构建与 API 级验证；完整的项目级 Agent Profile 实体、默认继承链比较与管理页仍待继续实施。
- 2026-04-05 补充实施进展：T-09 第二阶段已接通最小实体链（`agent_profiles` 真表、`/agent-profiles` CRUD、workspacePath 级 current profile 查询、`useSessions.newSession()` 继承、ChatTopBar“保存为项目配置”入口），并已通过 `useSessions.test.tsx`、`ChatPage.test.tsx`、gateway/web-client/Web 构建与 API smoke 验证。
- 2026-04-05 补充实施进展：T-09 第三阶段已接通设置页中的项目配置档列表/删除入口，并已通过 `connection-tab-content.test.tsx`、Web 构建与现有 `agent_profiles` 实体链整体验证。
- 2026-04-05 补充实施进展：T-10 已按 MVP 收口为“恢复链显式验收完成”（gateway `stream-attach-route` / `stream-resume-reconcile` / permission request binding、web `useGatewayClient` / `stream-recovery` / pending permission/question UI、浏览器级 `chat-stream-attach-recovery.spec.ts` 刷新恢复场景），当前阶段不额外扩展 Checkpoint UI。
- 2026-04-05 补充实施进展：T-10 第二阶段已接通最小外显层（`SessionRunStateBar` 的“查看恢复策略”、右侧概览中的恢复策略区块与 checkpoint/审批/问题统计），并已通过 `ChatPage.test.tsx`、`right-panel-sections.test.tsx`、Web 构建与真实浏览器恢复会话页面验证。
- 2026-04-05 补充实施进展：T-11 已接通 Phase 1 MVP（`notifications` 真表、关键 run-event 自动落库、`/notifications` 列表与已读 API、Layout 顶栏站内通知中心），并已通过 `notification-store.test.ts`、`Layout.test.tsx`、gateway/web 构建验证；浏览器 Notification API 仅做能力占位，完整推送偏好与多渠道投递继续后续实施。
- 2026-04-05 补充实施进展：T-11 第二阶段已接通浏览器通知触发（标签页不可见时对新未读通知执行 `new Notification(...)`），并已通过 `Layout.test.tsx` 的浏览器通知回归与 Web 构建验证。
- 2026-04-05 补充实施进展：T-11 第三阶段已接通最小通知偏好矩阵（gateway `notification_preferences` 读写、`GET/PUT /notifications/preferences`、web-client 偏好客户端、Security tab 通知偏好面板、Layout 按事件类型过滤浏览器通知）；当前继续保持“站内通知中心永远保留，偏好只控制页面隐藏时的浏览器提醒”这一最小产品语义，并进一步收口为“设置页显式申请浏览器提醒权限 + 偏好加载失败时浏览器提醒 fail-closed”，已通过 `notification-store.test.ts`、`notifications-routes.test.ts`、`Layout.test.tsx`、`SettingsPage.test.tsx`、gateway/web-client/web 构建验证。
- 2026-04-05 补充实施进展：T-12 第二阶段已接通最小共享会话链（`session_shares` 真表、`/team/session-shares` 列表/创建/删除 API、TeamPage 共享会话区块与权限级别 `view/comment/operate`），并已通过 `team-collaboration.test.ts`、`TeamPage.test.tsx`、gateway/web/web-client 构建与 API smoke 验证。
- 2026-04-05 补充实施进展：T-12 第三阶段已接通共享权限管理与协作审计流（gateway `team_audit_logs` 真表、`PATCH /team/session-shares/:id`、`GET /team/audit-logs`、web-client 审计/权限更新客户端、TeamPage 中的共享权限即时更新与协作审计面板），当前产品语义收敛为“先把共享权限调整与审计留痕做成真实能力，再把 presence/共享编辑/审批流留到后续阶段”，并已通过 `team-collaboration.test.ts`、`TeamPage.test.tsx`、gateway/web/web-client 构建验证。
- 2026-04-05 补充实施进展：T-12 第三阶段已根据实现后审查继续收口两个边界：`PATCH /team/session-shares/:id` 在 no-op 更新时保留原始 `updatedAt`，避免伪造“最近同步时间”；TeamPage 的成员/任务/消息/共享创建表单现在只在成功后清空，失败时保留用户输入，已补齐定向回归并重新通过 gateway/web/web-client 构建验证。
- 2026-04-05 补充实施进展：T-12 共享记录语义已进一步调整为 **workspace-aware**：team 路由从 `sessions.metadata_json.workingDirectory` 派生 `workspacePath` 并下发到 `session-shares` 返回值，TeamPage 的共享记录卡片与“共享当前会话”选择器都会显式展示工作区，解决同名会话在不同工作区下不可区分的问题；已通过 `team-collaboration.test.ts`、`TeamPage.test.tsx` 与既有 gateway/web/web-client 构建验证。
- 2026-04-05 补充实施进展：workspace-aware 调整在实现后审查中继续收口了列表投影回归：`GET /team/session-shares` 的 SQL 现已显式补齐 `sess.metadata_json AS session_metadata_json`，避免刷新列表后 `workspacePath` 丢失；并新增定向测试断言锁定该 SQL 投影，已重新通过 gateway/web 测试与 gateway/web/web-client 构建验证。
- 2026-04-05 补充实施进展：在共享记录 workspace-aware 的基础上，已继续落地 **共享给我的会话 + 只读访问闭环**：gateway 新增 `listSharedSessionsForRecipient()` / `getSharedSessionForRecipient()` helper、`GET /sessions/shared-with-me`、`GET /sessions/shared-with-me/:sessionId`；后端 shared detail 返回完整只读 session 读模型，Web 端 TeamPage 再把它消费成“共享给我的会话”面板中的只读消息预览。当前语义收敛为“先让 `view/comment/operate` 三档都具备真实可读能力，再把评论/操作能力继续分层下放到后续阶段”，并已通过 `session-shared-access.test.ts`、`team-collaboration.test.ts`、`TeamPage.test.tsx`、gateway/web/web-client 构建验证。
- 2026-04-05 补充实施进展：shared read access 在实现后审查中继续收口了 route-level 覆盖与模块边界：shared endpoints 已从超大 `sessions.ts` 中抽成独立 `session-shared-read-routes.ts`，并新增 `session-shared-read-routes.test.ts` 锁定 `/sessions/shared-with-me` 列表、详情和 404 行为；当前共享读取闭环已通过 helper 单测 + route-level 网关测试 + TeamPage 测试 + gateway/web/web-client 构建的四层验证。
- 2026-04-05 补充实施进展：final review 继续收口了 shared detail 的状态一致性：`GET /sessions/shared-with-me/:sessionId` 现在会让 `share.stateStatus` 与已 reconcile 的 `session.state_status` 保持一致，不再出现同一次响应里摘要状态和正文状态短暂漂移的情况；已通过更新后的 `session-shared-read-routes.test.ts`、gateway 测试与构建验证。
- 2026-04-05 补充实施进展：共享权限的下一层 **comment 能力** 已落地：gateway 新增 `shared_session_comments` 真表与 `session-shared-comment-store.ts`，`POST /sessions/shared-with-me/:sessionId/comments` 现在会严格限制为 `comment/operate` 可写、`view` 只读；shared detail 响应会一并返回评论流，TeamPage 的“共享给我的会话”面板新增评论列表与评论输入框（仅 comment/operate 可见）。本轮已通过 `session-shared-comment-store.test.ts`、`session-shared-read-routes.test.ts`、`session-shared-access.test.ts`、`team-collaboration.test.ts`、`TeamPage.test.tsx` 以及 gateway/web/web-client 构建验证。
- 2026-04-04 补充实施进展：T-08 已接通 `/workflows` Web MVP（模板库 + 画布 + 模板保存/删除），T-12 已接通 `/team` Web MVP（成员 / 任务 / 消息协作工作台），两者均已补齐路由、导航入口、自动化测试和 Web 构建验证。
- 2026-04-04 补充实施进展：T-06 已接通 Phase 1 MVP（SQLite FTS5 `session_messages_fts`、`/sessions/search`、Cmd+K 全局搜索会话内容、legacy/import session 惰性水合），并已完成自动化测试、构建与真实浏览器检索跳转验证。

Memory sync: completed
