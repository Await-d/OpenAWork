# 前两天功能恢复补全

## Task Overview
恢复 2026-03-27 与 2026-03-28 两天内与用户确认需要的 1–7 条功能线：聊天流式/停止/会话切换运行态、子代理任务链路、settings/devtools、settings/channels/workspace 设置能力、会话与工作区绑定/树状关系/删除、聊天 HTML/TSX/file preview、页面切换动画/路由缓存/预加载。

## Current Analysis
- 已经从本地 OpenCode snapshot 恢复了基线源码，并回放了部分 03-28 / 03-29 会话。
- 03-27 仍有多条大型主会话未做整会话定向回放。
- 当前要求是对 1–7 整体恢复，而不是再做范围筛选。
- 风险点在于：较早会话回放可能覆盖较晚修复，因此需要按时间顺序回放，并以两天内目标功能线的“最新文件体”为准。

## Solution Design
1. 锁定 03-27/03-28 与 1–7 功能线相关的主源会话。
2. 按消息时间顺序从 `summary.diffs` 提取这些会话的最新文件体，过滤 `node_modules/dist/tsbuildinfo/.evidence/数据库/报告` 等噪音后回放到工作区。
3. 对回放后出现的少量编译断点做收口修复。
4. 重新跑 LSP、`@openAwork/web build`、`@openAwork/agent-gateway build`、相关回归测试与 dev 启动验证。
5. 结束前做只读复核，确认这 1–7 条功能线已实质恢复。

## Complexity Assessment
- Atomic steps: 5+ → +2
- Parallel streams: yes → +2
- Modules/systems/services: 3+ → +1
- Long step (>5 min): yes → +1
- Persisted review artifacts: yes → +1
- OpenCode available: yes → -1
- **Total score**: 6
- **Chosen mode**: Full orchestration
- **Routing rationale**: 这是一次跨前端、网关、共享包与历史会话恢复的多主线回放任务，必须分阶段执行并做完整验证。

## Implementation Plan

### Phase 1: 锁定并回放源会话
- [ ] T-01: 选出 03-27/03-28 与 1–7 功能线相关的主源会话集合
- [ ] T-02: 按时间顺序回放这些会话的 `summary.diffs` 到工作区

### Phase 2: 收口回放后的不一致
- [ ] T-03: 修复回放后残余的类型/导出/签名不一致

### Phase 3: 验证
- [ ] T-04: 运行 LSP 诊断
- [ ] T-05: 运行 web/gateway build
- [ ] T-06: 运行针对 1–7 功能线的关键回归测试与 dev 启动

## Notes
- 当前重点是功能恢复，不做 git 提交。
- 回放仅作用于项目源文件，排除生成物与依赖目录。
