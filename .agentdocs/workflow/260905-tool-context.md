# 工具结果上下文治理
## 复杂度评估
跨三个环节 +1、独立并行 +2、长步骤 +1、证据持久化 +1，总分 5，完整编排。
## 目标与方案
只读分析指定本地会话；模型投影统一短预览和可读取引用；累计结果预算覆盖全部工具；保留错误、配对和近期图像；真实会话离线重放验证。不更改用户会话和现有工作区改动。
## 任务
- [x] T1 定位会话和旧/新链路差异
- [x] T2 模型投影大小上限与回归测试
- [x] T3 累计预算与全工具覆盖
- [x] T4 实际会话重放、类型检查及相关回归
## 初始证据
microcompact 生产函数：bash×4×190000 字符、mcp_call×30×70000 字符均未触发。旧链路 8KB 引用未进入新链路。存储同时有 output/rawOutput，存储大小不等同模型输入。

## 完成证据
- 指定会话模型入口工具字符从 1,252,144 降至 40,273（累计预算后），最大单条从 200,046 降至 7,569。
- 真实 `read_tool_output` 读取 200,026 字符记录时，响应 6,428 bytes 并返回 nextCharStart。
- 相关 59/59、route/atomic 7/7；gateway build exit 0。全量单测首次运行的三个旧占位断言已更新。
- typecheck 剩余 4 个错误来自并行存在的 `tool-sandbox-permission-audit.test.ts`，未改该文件。
## 复查复杂度评估
- 五条独立审查流 +2
- 跨消息/压缩/工具/Provider模块 +1
- 实际运行超过5分钟 +1
- 结果持久化 +1
- 总分：5，完整复查编排

## 最终复查与补漏
- [x] 工具图片计入累计预算；唯一/最新工具的超预算合成图片也会移除。
- [x] 工具附件最多四张；超过 500,000 字符的内联 Data URI（scheme 大小写均覆盖）不进入模型视图，持久化附件不变。
- [x] `skill`、MCP、网络、桌面、未知工具默认全部进入 48,000 字符累计预算；仅调用方显式配置时才保护特定工具。
- [x] `callCompactionLlm` 入口补充单条 8,192 字符投影及累计微压缩，消除辅助 LLM 旁路。
- [x] `read_tool_output` 字符分页避免切断 UTF-16 代理对，多页可无损重组 emoji。
- [x] 超长调用 ID、敏感尾部、参数化/URL-safe/含空白 Base64、跨用户读取均有回归覆盖。

## 最终证据
- 指定会话只读重放：84 条消息、31 条工具结果；模型投影工具字符 134,702，最大单条 7,569；累计预算后 37,639，清理 20 条，触发原因为 count。
- 当前工作树最终专项套件：10 个测试文件、78 项全部通过；Gateway build 退出码 0。
- 包级 typecheck 的剩余 4 个错误仅位于并行新增的 `tool-sandbox-permission-audit.test.ts`，未修改该文件。
