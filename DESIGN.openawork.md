# OpenAWork 设计补充说明

## 目的与阅读顺序

`DESIGN.md` 提供的是基于 `linear.app` 的通用设计基线。它定义了整体的暗色优先、轻边框、低饱和、克制强调色和高密度信息组织方式。

本文件定义的是 **OpenAWork 的项目级适配规则**。它回答三个问题：

1. 哪些 Linear 风格应该继承。
2. 哪些 Linear 特征不应照搬。
3. 在 OpenAWork 的具体页面、组件和交互里应该如何落地。

### 冲突优先级

当 `DESIGN.md` 与本文件冲突时，**以本文件为准**。

### AI Agent 使用顺序

进行任何 UI 设计或前端实现前，按以下顺序读取：

1. `AGENTS.md`
2. `DESIGN.md`
3. `DESIGN.openawork.md`
4. `packages/shared-ui/DESIGN-TOKENS.md`
5. 目标页面与 `packages/shared-ui` 中已存在的相似实现

---

## OpenAWork 从 Linear 借什么

OpenAWork 应借的是 **结构与控件语言**，不是品牌外观本身。

### 1. 深色原生的层级系统

- 页面底色使用近黑色深色背景。
- 侧栏、面板、弹层、卡片通过亮度微差形成层级。
- 层级优先靠背景明度递进，而不是靠重阴影或强色块。

### 2. 高密度但不混乱的信息组织

- 列表、面板、工具栏、状态区可以密集，但必须清晰分组。
- 一级信息优先通过位置与字号区分，二级信息通过颜色减弱，不靠堆砌边框。
- 默认视图优先服务“连续工作”，而不是品牌展示。

### 3. 克制而稳定的组件语言

- 细边框。
- 小到中等圆角。
- 少量强调色。
- 深色背景上的微弱分隔线。
- 统一而低噪声的 hover、focus、active 状态。

### 4. 工程化的排版节奏

- 主字体使用 Inter。
- 文字权重按 400 / 510 / 590 三段使用：
  - 400：正文与说明
  - 510：导航、标签、次级强调
  - 590：标题、重点信息
- 技术信息、日志、代码、模型标识、执行元数据可使用等宽字体，但只限技术语境。

---

## OpenAWork 不应照搬什么

### 1. 不照搬 Linear 的品牌复制

- 不复制 Linear 品牌感文案。
- 不复制 Linear 的 logo、品牌表达、营销语言。
- 不把页面做成“像 Linear 官网”。

OpenAWork 是 AI Agent 工作台，不是营销站。

### 2. 不照搬过强的营销页节奏

- 不在产品内页滥用 48px 以上超大标题。
- 不在工作台页面使用大段空旷留白来制造“高级感”。
- 不让功能页面看起来像 landing page。

### 3. 不把强调色变成全局装饰色

- OpenAWork 的主强调色为靛青（`#5cd4c0` 暗 / `#6471f0` 亮），对比色为琥珀。
- 强调色只能用于：
  - 主要 CTA
  - 选中状态
  - 活跃导航
  - 可交互重点
- 不允许把强调色用于大面积背景装饰或无语义点缀。
- 琥珀色仅用于 warning 语义和数据对比高亮，不做装饰。

### 4. 不丢失 OpenAWork 自己的语义颜色

OpenAWork 有大量工作台状态，不适合把所有颜色都压成黑白紫三色。

以下语义必须保留：

- 成功 / 完成
- 运行中 / 流式中
- 警告 / 风险
- 错误 / 失败
- 信息 / 提示
- Agent 类型 / 工具类型 / 日志等级

这些颜色应该克制，但不能消失。

---

## OpenAWork 的产品优先级

所有 UI 设计都应优先服务以下目标，而不是单纯追求某种风格一致性。

### 1. 连续工作优先

用户会长时间停留在工作台内进行：

- 聊天
- 查看工具调用
- 跟踪工作流
- 管理会话
- 读取日志
- 检查诊断结果
- 编辑配置

因此界面必须耐看、稳定、低干扰，而不是短时间惊艳。

### 2. 跨平台一致性优先

OpenAWork 同时覆盖：

- Web
- Desktop
- Mobile

因此设计语言必须满足：

- 相同语义在不同平台表现一致
- 组件结构尽量共享
- 面板逻辑、状态色和优先级保持一致

### 3. AI 工作流可读性优先

相较于通用 SaaS，OpenAWork 更强调：

- 流式内容
- 多步骤状态
- 工具调用结果
- 运行过程可追踪
- 输出产物可管理

因此需要优先保证：

- 时间顺序清晰
- 状态切换明显
- 元信息易扫读
- 错误与恢复路径可见

### 4. 控制台感优先于装饰感

OpenAWork 更像“代理控制台 + 工作台”，而不是品牌秀场。

在风格取舍上，默认优先：

- 清晰结构
- 低噪声层级
- 快速操作
- 可维护性

而不是：

- 大量渐变
- 夸张玻璃拟态
- 装饰性发光
- 无语义动画

---

## 页面级落地规则

### Chat / 会话主工作区

- 整体骨架保持稳态工作台结构，不做内容营销页化。
- 消息区可以比其他区域更柔和，但不能脱离全局深色层级系统。
- 工具调用、思考状态、附件、产物卡片必须比普通消息更利于扫读。
- 对话氛围可少量借鉴 `claude` 的温和感，但骨架、边框、按钮仍遵循本项目规则。

### Sessions / 会话管理页

- 重点是列表、筛选、分组、状态与更新时间。
- 优先使用清晰的密度与层级，不靠插画和装饰区分状态。
- 当前活跃项、最近项、异常项必须一眼可辨。

### Workflows / 工作流页

- 节点、步骤、依赖和状态是核心。
- 面板与画布应强调结构秩序，避免花哨背景抢焦点。
- 成功、阻塞、等待、失败应通过语义色和图标双重表达。

### Settings / 配置页

- 采用稳定、系统化、可扫描的控制台布局。
- 区块标题、说明文、表单、帮助文本层级必须明确。
- 默认优先读写效率，不追求“卡片商城式”的视觉表演。

### Usage / Diagnostics / Logs

- 这些页面优先“监控与排障”体验。
- 数据、时间、状态、来源、严重级别必须易于横向比较。
- 日志与诊断区域允许更强的等宽字体使用，但仅限元数据和正文日志。

### Artifacts / 产物页

- 产物列表要兼顾预览与管理。
- 元信息应紧凑但不拥挤，支持快速判断类型、状态、来源与更新时间。
- 卡片式布局可以使用，但视觉重点必须服务于检索和操作，不服务于展示。

---

## 组件级落地规则

### Buttons

- 默认按钮采用低噪声深色按钮样式。
- 主按钮使用唯一主强调色。
- 次按钮、ghost 按钮和工具栏按钮保持克制，不与主按钮抢层级。
- 删除、危险操作必须有独立风险语义，不能仅靠灰度变化表达。

### Cards / Panels

- 默认使用 8px 卡片圆角、12px 面板圆角。
- 卡片背景与页面背景应存在轻微亮度差。
- 卡片边框始终偏轻，避免高对比描边造成噪声。
- 大面板可使用更柔和的内层级，不要叠加过度阴影。

### Inputs / Forms

- 输入框默认 6px 圆角。
- 背景与卡片保持同体系，但必须保证可编辑区域清晰。
- focus 状态优先通过轻边框与清晰 ring 表达，不使用夸张 glow。
- 帮助文本、校验提示、占位符必须与正文拉开层级。

### Badges / Pills

- badge 只表达状态、类别、数量、等级，不做无意义装饰。
- 中性标签使用深色背景 + 轻边框。
- 状态标签必须保留语义色，但饱和度应低于营销型产品。

### Command Palette / 快捷操作

- 这里允许更明显地借鉴 `raycast`：更强的效率感、更紧凑的命令列表、更清晰的快捷键提示。
- 但颜色、边框、字体层级仍需保持和全局系统一致。
- Command Palette 是“高效率入口”，不应成为视觉上最花哨的模块。

### Code / Log / Technical Metadata

- 使用等宽字体。
- 保持技术感，但避免过度终端化影响可读性。
- 代码、命令、模型、token、耗时、trace、日志等级等信息要有统一呈现方式。

---

## 正式设计 Token（E · Nebula 定稿）

以下为 OpenAWork 正式采用的设计 token。所有 UI 实现必须严格引用这些变量，禁止硬编码色值。

### 色彩哲学

- **主强调色（accent）**：靛青 — 用于 active / CTA / 选中态
- **对比色（contrast）**：琥珀 — 用于 warning / 次级强调 / 数据高亮（冷暖对比）
- **互补色（complement）**：珊瑚 — 用于 danger / destructive（色环对面）
- **辅助色（aux）**：靛蓝 — 用于 info / 链接 / 代码高亮 / read 类工具

每种色都有 4 个层级：实色 / muted(14%) / subtle(7%) / border(30%)

### 暗色主题

```
bg-base: #080b12          bg-raised: #0d1119
bg-overlay: #121721       bg-surface: #171d29
bg-elevated: #1d2535      bg-hover: #232d40

fg-strong: #f1f4f8        fg-default: #c8d1e0
fg-muted: #7b8a9e         fg-subtle: #4d5b6e

accent: #5cd4c0           contrast: #f0b429
complement: #f06b7e       aux: #8b9cf5
success: #3dd49a

border-invisible: hsla(215,20%,50%,0.03)
border-subtle: hsla(215,20%,50%,0.07)
border-default: hsla(215,18%,50%,0.12)
border-emphasis: hsla(215,16%,55%,0.20)
border-strong: hsla(215,14%,60%,0.30)
```

### 亮色主题

```
bg-base: #f4f6ff          bg-raised: #ffffff
bg-overlay: #eef1ff       bg-surface: #e4e8fa

fg-strong: #161a3a        fg-default: #43497a
fg-muted: #7c83a9         fg-subtle: #a8aec8

accent: #6471f0           contrast: #a06bff
complement: #e0497a       aux: #3aa0ff

border-default: rgba(15,23,60,0.08)
border-emphasis: rgba(15,23,60,0.14)
```

### 图表配色（8 色）

暗: `#5cd4c0` `#f0b429` `#8b9cf5` `#f06b7e` `#c4b5fd` `#86efac` `#67e8f9` `#fda4af`
亮: `#6471f0` `#a06bff` `#3aa0ff` `#e0497a` `#7c3aed` `#16a34a` `#0891b2` `#e11d48`

### 代码语法高亮

keyword=靛蓝 / function=靛青 / type=琥珀 / string=青柠 / number=玫瑰 / comment=subtle

### Radius

xs: 4px · sm: 6px · md: 8px · lg: 12px · xl: 16px · pill: 9999px

### 动效

- micro: 100ms `cubic-bezier(0.4, 0, 0.2, 1)`
- normal: 200ms `cubic-bezier(0.16, 1, 0.3, 1)`
- emphasis: 350ms `cubic-bezier(0.34, 1.56, 0.64, 1)`

### 间距

4 / 8 / 12 / 16 / 20 / 24 / 32 / 48 px（8px 基准网格）

### 阴影

- sm: 卡片默认（多层,含 inset 高光）
- md: 浮层/下拉
- lg: Modal/弹窗
- glow: CTA 强调（accent 色发光）


---

## 响应式与平台适配

### Web

- 桌面端优先保证多面板工作流清晰。
- 小屏时先折叠次级信息，不先压缩主任务区。

### Desktop

- 保持与 Web 相同的层级和组件语义。
- 标题栏、导航轨、命令入口可更偏 `raycast` 式效率工具感。

### Mobile

- 不试图把桌面多面板硬塞进单屏。
- 优先保证任务主线、关键状态、核心操作的可达性。
- 将详情、诊断、扩展元信息移入可展开层。

---

## 明确的 Do / Don't

### Do

- 先复用 `packages/shared-ui` 中的现有模式。
- 用亮度差管理层级。
- 用一个主强调色建立交互优先级。
- 用状态色表达运行语义。
- 让工作台页面始终服务于“连续工作”。

### Don't

- 不要把页面做成 Linear 官网复制版。
- 不要在工作台页面使用夸张 hero、巨型标题和营销排版。
- 不要把强调色拿去做背景装饰。
- 不要为了“高级感”牺牲诊断、日志、状态的可读性。
- 不要新造一套脱离 `shared-ui` 的平行组件视觉语言。

---

## 给 AI Agent 的最短执行指令

如果你正在为 OpenAWork 编写或修改 UI，请遵守以下规则：

1. 先读取 `DESIGN.md`，再读取本文件。
2. OpenAWork 的全局气质应是：**Linear 的秩序感 + Raycast 的工具效率感 + Claude 的局部聊天温度**。色彩体系为 E · Nebula（靛青主强调 + 琥珀对比 + 珊瑚互补 + 靛蓝辅助），详见"正式设计 Token"章节。
3. 全局骨架、面板、列表、设置、工作流、日志遵循本文件，不得营销页化。
4. 聊天区可以更柔和，但不能脱离全局 token、边框和层级系统。
5. 优先复用现有 shared-ui 组件与模式，不凭空发明新视觉体系。
