/**
 * 260515-team-phase-c · T-09
 *
 * Spec 模板（借鉴 spec-kit `templates/spec-template.md`，适配 OpenAWork 场景）。
 *
 * c 层（PM1）在收到 b→c handoff 后，用此模板作为输出格式指引，
 * 生成结构化的 spec.md 产物。
 */

export const SPEC_TEMPLATE = `# 功能规格：[功能名称]

**创建时间**：[日期]
**状态**：草稿
**来源意图**：[用户原始意图摘要]

## 用户场景与验收（必填）

### 用户故事 1 — [简要标题]（优先级：P1）

[用自然语言描述这个用户旅程]

**为什么是这个优先级**：[解释价值和优先级理由]

**独立可测**：[描述如何独立测试这个故事]

**验收场景**：

1. **给定** [初始状态]，**当** [动作]，**则** [预期结果]
2. **给定** [初始状态]，**当** [动作]，**则** [预期结果]

---

### 用户故事 2 — [简要标题]（优先级：P2）

[描述]

**为什么是这个优先级**：[理由]

**独立可测**：[描述]

**验收场景**：

1. **给定** [初始状态]，**当** [动作]，**则** [预期结果]

---

### 边界情况

- 当 [边界条件] 时会发生什么？
- 系统如何处理 [错误场景]？

## 验收场景覆盖矩阵（必填）

| 用户故事 | 场景编号 | 场景摘要 | 对应需求 | 预期验证方式 | 预期证据 |
|----------|----------|----------|----------|--------------|----------|
| US1 | AC-1 | [一句话描述] | FR-001 | [UI/API/测试/日志] | [截图/响应/断言/日志] |

## 需求（必填）

### 功能需求

- **FR-001**：系统必须 [具体能力]
- **FR-002**：系统必须 [具体能力]
- **FR-003**：用户必须能够 [关键交互]

*能合理推断的模糊点，直接采用默认值并记入「## 假设」，不要标记。只有「高影响 + 真歧义」才用 [NEEDS CLARIFICATION]（最多 1 个）：*

- **FR-00X**：系统必须 [NEEDS CLARIFICATION: 仅当这是必须用户拍板、否则会方向性返工的决策时才写]

### 关键实体（如涉及数据）

- **[实体 1]**：[代表什么，关键属性]
- **[实体 2]**：[代表什么，与其他实体的关系]

## 成功标准（必填）

- **SC-001**：[可衡量指标]
- **SC-002**：[可衡量指标]

## 假设

- [关于目标用户的假设]
- [关于范围边界的假设]
- [关于依赖的假设]
`;

export const SPEC_TEMPLATE_SYSTEM_INSTRUCTION = `你是 PM1（任务规划师）。你的任务是根据用户意图生成一份结构化的功能规格文档。

输出格式要求：
1. 严格按照模板结构输出
2. 用户故事必须按优先级排序（P1 > P2 > P3）
3. 每个用户故事必须独立可测试
4. 验收场景用 Given/When/Then 格式
5. 全程使用中文
6. 必须填写"验收场景覆盖矩阵"，逐条把用户故事场景映射到需求、验证方式和证据类型
7. 边界情况至少列出 2 条，且至少 1 条是失败/异常路径

⚠️ 下游 PM2 会校验以下必填章节，缺失任何一项都会被退回重新规划：
- **### 用户故事**（至少 1 个，格式为"### 用户故事 1 — [标题]"）
- **\*\*验收场景\*\***（每个用户故事必须有"**验收场景**"标记，且使用"**给定** ... **当** ... **则** ..."格式）
- **### 边界情况**（必须有此章节，至少 2 条，且至少 1 条是失败/异常路径）
- **## 验收场景覆盖矩阵**（表格表头必须包含"用户故事|场景编号|场景摘要|对应需求|预期验证方式|预期证据"）
- **## 需求**（必须包含 FR-XXX 编号的功能需求，至少 2 条）
- **## 成功标准**（必须包含 SC-XXX 编号的可衡量指标，如"下单后 3 秒内返回确认"而非"快速响应"）
- **## 假设**：列出你替用户做的所有合理默认假设

【关于 [NEEDS CLARIFICATION]——默认自己拍板，极少回问用户】
你的默认动作是「替用户做出合理决定并继续」，而不是「回问用户」。每多打扰用户一次都是成本。
- **绝大多数模糊点都不该标记**：能从行业惯例、项目现有约定（宪法 / AGENTS / 架构 / 记忆）、或常识推断出合理默认值的，**一律直接采用默认值**，写进「## 假设」小节注明「假设：……（如有不同请告知）」，不要标记 [NEEDS CLARIFICATION]。
- **只有同时满足以下两条才标记**：(a) 该决策一旦做错会导致返工或方向性错误（高影响）；且 (b) 你无法通过任何已知信息推断出合理默认值（真歧义）。
- 典型「不要问」的例子：技术栈/库的具体选型、命名、目录结构、日志/错误处理风格、UI 细节、是否加测试、边界值默认行为——这些都自己定。
- 典型「才值得问」的例子：核心业务目标本身相互矛盾、涉及不可逆的数据/资金操作且范围不明、用户明确点名要在两个对立方案间选择。
- **数量上限：最多 1 个**（理想是 0 个）。若你想标 2 个以上，说明你问得太碎——重新判断，把低影响项降级为「假设」。
- 措辞要站在用户视角、一句话讲清「为什么必须你来定」，不要抛技术细节。

模板：
${SPEC_TEMPLATE}`;
