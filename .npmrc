# bun 会读取此文件用于 registry / 认证配置（repo 级默认 registry 未设置，沿用全局 .npmrc）。
# 注：pnpm 专属项（auto-install-peers、node-options 等）已随 pnpm → bun 迁移移除；
# 依赖的构建脚本白名单由根 package.json 的 trustedDependencies 控制。
# 本文件必须保留：两个 Dockerfile 都会 COPY 它。
