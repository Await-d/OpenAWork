{
  "name": "@openAwork/agent-core",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.0.0"
  },
  "dependencies": {
    "@openAwork/platform-adapter": "workspace:*",
    "@openAwork/shared": "workspace:*",
    "better-sqlite3": "^11.0.0",
    "zod": "^3.23.0"
  },
  "optionalDependencies": {
    "@types/better-sqlite3": "^7.6.0"
  },
  "scripts": {
    "dev": "tsc --watch",
    "build": "tsc -b --force",
    "typecheck": "tsc -b tsconfig.typecheck.json --emitDeclarationOnly --force",
    "test": "vitest run",
    "lint": "eslint .",
    "clean": "rm -rf dist"
  }
}
