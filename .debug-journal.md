# Debug Journal — 提交前验证收口

Started: 2026-08-17T14:44:33+08:00
Goal: 修复非测试代码错误，并依照用户指示移除仅由测试夹具导致失败的验证入口，使工作区达到可推送验证条件。

## Environment snapshot (Phase 0)

- Runtime: Node.js v26.2.0; pnpm 10.25.0; `tsx` verification launcher。
- Entry: `pnpm --filter @openAwork/agent-gateway run test:verification`。
- Ports: Node inspector 9229/9230 均未监听。
- Git HEAD: `5172ba2862baa7fbdd7d5ae750991a9e710ed553` on `main`; working tree had 161 user-owned entries before this session.
- References read: Node runtime; debugging phases 0, 2, 3, 6, 8, 9; TypeScript programming guide.

## Hypotheses

1. [CONFIRMED] `verify-session-file-summary-routes` 在 Windows 用 POSIX `/tmp` 并以 `rmSync` 清理，断言通过后遇到 EPERM。Evidence: line 837 and real residual `E:\\tmp\\openawork-session-summary-*`. Fix: remove verifier.
2. [CONFIRMED] Production cross-platform path parsing/normalization depends on host path flavor, causing Gateway skill/repository behaviors to fail on Windows. Evidence: isolated unit-test and Node path probes. Fix: flavor normalization.
3. [CONFIRMED] Production TypeScript template/shell wrapper defects cause parser errors and missing child-process stdout. Evidence: isolated test failures and direct PowerShell probe. Fix: escape template; preserve stdout.

## Failed hypothesis round counter

- Round 1: not applicable; all listed hypotheses have direct runtime evidence.

## Artifacts to revert

- [ ] `.debug-journal.md` and `.git/info/exclude` entry — remove at cleanup with `apply_patch`.

## Findings

- 2026-08-17: `verify-session-file-summary-routes` assertions print `ok`; only final workspace cleanup fails with Windows EPERM.
- 2026-08-17: Oracle investigation separated production defects from test-fixture-only failures before edits.

## Final fix

- Pending implementation and final verification.

## 2026-08-30 OpenAI 思考等级矩阵视觉复拍

- Goal: 在 settings/connection 当前源码上重拍 OpenAI 思考控件，并完成新鲜双重视觉审查。
- Runtime: Node.js v26.2.0; Chromium via Playwright; Vite dev server expected at localhost:5173。
- Entry: `node .omo/evidence/openai-reasoning/browser-qa-refresh.cjs`。
- Hypotheses:
  1. [PENDING] Vite 实际入口或路由不是 `http://localhost:5173/settings/connection`；distinguishing evidence: page.url/title/body and response status。
  2. [PENDING] 页面脚本加载失败或运行时抛错，导致“模型与提供商”未渲染；distinguishing evidence: pageerror/console/network logs and body text。
  3. [PENDING] settings providers 请求未命中 fixture，页面停留在加载/空态；distinguishing evidence: intercepted request count and DOM loading markers。
- Artifacts to revert/clean:
  - [ ] `.omo/evidence/openai-reasoning/connection-*.png` and `controls-*.png` — overwrite only with fresh screenshots from the current build。
  - [ ] `.omo/evidence/openai-reasoning/browser-qa-refresh.cjs` — existing QA script may be adjusted only for capture diagnostics; retain if it is the final capture tool。
  - [ ] `.debug-journal.md` — remove this session section only if the session artifacts are discarded; otherwise retain as local journal。

## 2026-08-17 CI lint investigation

- Goal: 修复 GitHub Actions run 32006110891 / job 95315923953 的 agent-gateway lint 失败。
- Runtime: Node.js v26.2.0; pnpm 10.25.0; CI command `pnpm --filter "@openAwork/shared" --filter "@openAwork/agent-core" --filter "@openAwork/agent-gateway" --filter "@openAwork/shared-ui" --filter "@openAwork/web-client" --filter "@openAwork/web" --filter "@openAwork/skill-types" --filter "@openAwork/mcp-client" --filter "@openAwork/lsp-client" --filter "@openAwork/multi-agent" --filter "@openAwork/mobile" --filter "@openAwork/desktop" lint`.
- Git HEAD: `611a105e1764471ce5d2e4d01813e0255b10b69c`; working tree clean before this journal update.

### Hypotheses

1. [PENDING] `opencode-llm` 的声明/生成文件无法被 agent-gateway 的 TypeScript ESLint 项目正确解析，导致导入类型退化为 error type。Distinguishing evidence: direct `tsc`/ESLint diagnostics and package declaration imports. Fix: repair package declarations or build ordering.
2. [PENDING] agent-gateway 的 ESLint 类型项目配置把 workspace source 与 generated declarations 混用，导致跨包类型被标记为 error type。Distinguishing evidence: same source passes `tsc` but fails ESLint, and changing parser project scope toggles the diagnostics. Fix: correct ESLint project scope.
3. [PENDING] CI uses a clean checkout where generated `opencode-llm` files differ from local tracked outputs or are absent, while local state masks the error. Distinguishing evidence: clean checkout/build and package file inventory reproduce the same import errors. Fix: make generated artifacts/build dependency explicit.

### Artifacts to revert

- [ ] `.debug-journal.md` appended CI investigation; remove the full debug journal at cleanup if it was created only for this session.
- [ ] `C:\Users\28528\AppData\Local\Temp\openawork-ci-repro-927837d19f074559abc52ea643f52656` — detached clean worktree to reproduce CI; remove with `git worktree remove --force <path>` after the final verification.
- [ ] `C:\Users\28528\AppData\Local\Temp\openawork-ci-repro-927837d19f074559abc52ea643f52656\packages\opencode-llm\dist` — regenerated solely by the clean-worktree typecheck probe; remove with `pnpm --filter @openAwork/opencode-llm clean` before worktree cleanup.
- [ ] `C:\Users\28528\AppData\Local\Temp\openawork-ci-fixed-repro-6ae57ab74efa493fb0f53d28d126521e` — detached clean worktree for the fixed CI sequence; remove with `git worktree remove <path>` after verification.

## Root cause (confirmed 2026-08-17)

- Mechanism: `@openAwork/opencode-llm` exports only `./dist/*`, while `dist/` is ignored and absent in a fresh checkout. The CI quality job lints agent-gateway before any command builds that dependency, so TypeScript ESLint resolves its imports as error types and the 22 `no-redundant-type-constituents` diagnostics follow. The current developer checkout silently masks this because it retains `dist` from prior builds.
- Evidence: the clean detached worktree at `611a105e` reproduces the CI's exact lint failure after `pnpm install --frozen-lockfile`; the primary worktree, where `packages/opencode-llm/dist/index.d.ts` exists, passes the same gateway lint command.
- Toggle proof: a new clean worktree without `packages/opencode-llm/dist` fails the CI lint command; after building that exact package, the unchanged command passes. A Windows path-length error prevented deletion of the ignored temporary `node_modules` tree after the test, but does not affect the observed absent-versus-present build toggle.
- Fix scope: `.github/workflows/ci.yml`; build the workspace dependency before each CI job that consumes the gateway package.

## Findings

- CI red: on clean `611a105e`, `pnpm install --frozen-lockfile` followed by the exact CI lint filter exits 1 with the same 22 error types.
- CI green: on a second clean `611a105e` worktree, `pnpm --filter @openAwork/opencode-llm build` followed by that exact lint filter exits 0.
- CI-equivalent quality: the full CI typecheck filter exits 0 after the dependency build.
- Manual QA: `node --input-type=module -e "import('@openAwork/opencode-llm').then(() => console.log('loaded'))"` prints `loaded`; `pnpm --filter @openAwork/agent-gateway run test:chat:fast` passes 13 tests and `verify-stream-attach-recovery: ok`.
- Cleanup note: both temporary worktree registrations were removed. Windows reported path-length/non-empty-directory errors while deleting their ignored `node_modules` trees, so the two explicitly journaled Temp directories may remain and are safe to delete outside this session.

## 2026-08-30 对话异常重试指数退避

- Goal: 修复对话异常后的自动重试在短时间内快速触发，确保重试等待按指数退避增长。
- Runtime: Node.js v26.2.0; pnpm 10.25.0; Vitest/tsx。
- Entry: 待通过源代码调用链与定向测试确认；监听端口 3000 已有进程 14864，暂不干扰。
- Git HEAD: `e65efac4c2f033e2a53f58ea39fb3b9a4c336139`; 工作树含大量既有用户改动，禁止覆盖。
- References read: debugging Node runtime; methodology 00-setup/02-investigate/06-fix/08-qa/09-cleanup; programming TypeScript README/type-patterns/data-modeling/error-handling。

### Hypotheses

1. [PENDING] 自动对话重试调用了固定延迟或默认短延迟，未将当前重试次数传入 `computeRetryDelayMs`/`withRetry`。Distinguishing evidence: runtime/test spy shows identical inter-attempt delays or attempt always `1`. Fix: pass attempt state。
2. [PENDING] 后端已经计算指数退避，但流式对话的上层 catch/重试循环绕过了该等待，直接重新调用 provider。Distinguishing evidence: classifier tests pass while real stream call timestamps are near-zero and no sleep occurs. Fix: centralize wait。
3. [PENDING] 前端异常恢复控制器对每次错误都重置 retry count，因此 `Date.now()` 调度始终使用初始 delay。Distinguishing evidence: hook state/reset sequence shows retry count returns to zero between consecutive failures. Fix: preserve counter。

### Artifacts to revert

- [ ] 本节 journal 追加；最终保留既有 journal 内容，仅清理本次新增的临时调试工件。
- [ ] `services/agent-gateway/src/__tests__/v2-runtime/v2-runtime-upstream.test.ts` — add a failing regression test for configured stream retry/backoff; retain only if it guards the fix.
- [ ] `services/agent-gateway/src/v2-runtime/upstream/stream-runner.ts` — wire configured stream retries with cancellable exponential backoff; retain as the production fix.
- [ ] `services/agent-gateway/src/v2-runtime/upstream/stream-retry-policy.ts` — new pure retry timing seam if needed; retain only if the final implementation uses it.

### 调查发现（2026-08-30）

- `services/agent-gateway/src/routes/stream.ts:2962-2967` 在 session recovery 成功后直接 `continue`，当前没有 `await`、计时器或 retry attempt 状态；这条恢复分支会在异常后立即发起下一轮对话。
- `services/agent-gateway/src/v2-runtime/upstream/stream-runner.ts:46` 暴露 `maxRetries`，但 `:612-623` 构造原生请求时未使用该字段；上游流的可配置重试次数因此没有进入实际调用链。
- `apps/web/src/components/conversation-runtime/attach/use-stream-attach-retry.ts:44-55` 仅按调用方传入的固定 `delayMs` 调度，虽然文档称为指数退避，但没有 attempt 计数或指数计算；这是连接恢复的独立缺口。

### 计划新增/修改工件（2026-08-30）

- [ ] `services/agent-gateway/src/v2-runtime/upstream/stream-retry-policy.ts` — 新增上游流重试策略与可重试错误判定；完成后保留。
- [ ] `services/agent-gateway/src/session/session-retry-policy.ts` — 新增 session recovery 可取消指数等待；完成后保留。
- [ ] `services/agent-gateway/src/session/session-retry-policy.test.ts` — 新增 session recovery 退避边界测试；若最终不采用则删除。
- [ ] `services/agent-gateway/src/__tests__/v2-runtime/stream-runner-retry.test.ts` — 拆出上游重试集成回归测试，避免继续扩大既有大测试文件；完成后保留。
- [ ] `apps/web/src/components/conversation-runtime/attach/use-stream-attach-retry.ts` — 为重复 attach 失败保存 attempt 并计算指数延迟；完成后保留。
- [ ] `apps/web/src/components/conversation-runtime/attach/use-stream-attach-retry.test.tsx` — 新增 attach 重试退避测试；完成后保留。

### 红测证据（2026-08-30）

- 命令：`pnpm --filter @openAwork/agent-gateway exec vitest run src/__tests__/v2-runtime/v2-runtime-upstream.test.ts -t "retries transient upstream stream failures with exponential backoff" --no-file-parallelism`
- 结果：退出码 1；测试失败在首个调用断言：`expected "stream" to be called 1 times, but got 0 times`。
- 解释：当前测试在 `runUpstreamStream` 创建后的同步时刻断言，尚未让 `Effect.promise` 微任务运行；这不是生产实现是否重试的有效证据，需先修正测试等待边界后再重新记录行为。
- 修正测试并重跑后：首个上游调用在 `advanceTimersByTimeAsync(0)` 后发生；推进 1,999ms 再推进 1ms，第二次调用仍为 0（实际 `1` 次），测试在第二次调用断言失败。该结果确认 `maxRetries: 2` 当前未驱动流级重试/退避。

### 修复后证据（2026-08-30）

- `stream-runner-retry.test.ts`：4 个上游边界测试通过，覆盖 503 指数退避、Transport 错误退避、不可重试错误不重试、已有部分输出不重复。
- `session-retry-policy.test.ts`：3 个测试通过，覆盖 2s/4s/8s 延迟、定时器完成和 AbortSignal 取消。
- `v2-runtime` 相关目录 + session policy：19 个测试文件、141 个测试通过。
- `test:chat:fast`：stream-error/session-run-events 13 个测试通过，`verify-stream-attach-recovery: ok`。
- Web attach policy：2 个测试通过；Web typecheck 通过。
- 网关 ESLint（本次 6 个生产/测试文件）：通过；网关 typecheck 仍被既有 `automatic-compaction-orchestrator.ts:414` 的 `targetTokenRatio` 类型错误阻断。

## 2026-08-30 聊天输入框 Fast 模式联动 QA

- Goal: 验证聊天输入框模型设置中的 Fast 开关真实写入 OpenAI Provider 配置，并覆盖响应式布局。
- Runtime: Node.js v26.2.0; Chromium via Playwright 1.62.0; Vite dev server 127.0.0.1:5173。
- Artifacts to revert:
  - [ ] `C:\Users\28528\AppData\Local\Temp\openawork-fast-mode-qa.mjs` — temporary Playwright script。
  - [ ] `C:\Users\28528\AppData\Local\Temp\openawork-fast-mode-qa-*.png` — fresh viewport screenshots。
  - [ ] `C:\Users\28528\AppData\Local\Temp\openawork-fast-mode-gateway.sqlite` — isolated Gateway database。
  - [ ] `C:\Users\28528\AppData\Local\Temp\openawork-fast-mode-gateway-*.log` — isolated Gateway logs。
  - [ ] Gateway process listening on port 3001 — terminate after QA。

## 2026-08-30 提交前云端构建门禁收口

- Goal: 在提交并推送前消除本地可复现的云端阻断错误，确保发布提交触发构建。
- Runtime: Node.js v26.2.0; pnpm 10.25.0; Vitest 4.1.10; React/Vite。
- Git HEAD: e65efac4；当前工作树包含用户已有的大批改动，禁止回滚或覆盖。
- References read: debugging Node runtime; methodology 00-setup/02-investigate/06-fix/08-qa/09-cleanup; Playwright CLI; programming TypeScript README/type-patterns/error-handling; frontend。

### Hypotheses

1. [CONFIRMED] team inbound 测试收到 `xhigh` 是模型默认推理档位已由当前改动升级，而测试夹具仍固定断言 `high`。Distinguishing evidence: 两个失败断言均实际收到 `xhigh`，调用链经过当前 `reasoningEffort` 默认值与 provider 支持矩阵。
2. [CONFIRMED] 模板内联保存失败的状态保留逻辑存在，但测试未找到 alert 可能来自异步状态更新未完成或错误节点不在可达渲染分支。Distinguishing evidence: `TemplateDetailView.tsx:175-186` 已有 `role="alert"`，需用单测原始路径与 DOM 状态复现后定位。
3. [CONFIRMED] 两处 ESLint `restrict-template-expressions` 错误来自 `never` 分支的模板字符串插值，属于未修改文件但会阻断全仓 lint。Distinguishing evidence: lint 报告精确定位 `openai-chat.ts:379` 与 `openai-responses.ts:501`，需以类型安全方式移除对 `never` 的字符串化插值。

### Artifacts to revert/clean

- [ ] 本节 journal 追加；提交前保留为用户已有未跟踪文件，不执行 git 回滚。
- [ ] 本次若创建临时 Playwright 脚本、截图或日志，须在最终验证前清理。

### Findings

- `pnpm check:fastify-alignment`: passed。
- `pnpm typecheck`: passed。
- `pnpm format:check`: baseline failure，报告大量既有未格式化文件，不能作为本次改动单独阻断证据。
- `pnpm lint`: failed only at `packages/opencode-llm/src/protocols/openai-chat.ts:379` and `openai-responses.ts:501`。
- `pnpm test`: 1704 passed / 3 failed；失败为 team `reasoningEffort` 断言两项与 TemplateDetailView alert 一项。

### 修复后验证

- team conversation + TemplateDetailView 定向测试：2 个文件、42 个测试通过。
- `pnpm --filter @openAwork/web exec vitest run --no-file-parallelism`：282 个测试文件、1708 个测试通过。
- 首次并行 `pnpm test` 期间 `shared-ui-provider.test.ts` 单项失败；同一测试单独运行通过，安静串行全量 Web 复跑通过，归类为并行执行隔离波动而非稳定产品错误。
- `pnpm lint`：通过（含 lint:rules）。
- `pnpm build`：通过；Vite 仅输出既有动态导入与大 chunk 警告。
