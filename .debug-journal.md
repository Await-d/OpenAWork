# 调试记录 — 工具结果上下文预算复查

Started: 2026-09-05T00:00:00+08:00
Goal: 复查并修复所有工具调用结果导致模型上下文快速耗尽的旁路。

## 环境快照

- Runtime: Node.js + pnpm + TypeScript NodeNext + Vitest
- Entry: services/agent-gateway 模型消息构造、microcompact、compaction LLM
- Git HEAD: 见后续证据；工作区含用户并行修改
- References read: programming/typescript/README.md；debugging 00-setup、02-investigate、06-fix、08-qa、runtimes/node.md

## 假设

1. [待验证] 默认保护 skill 导致多个大结果完全绕过 48K 累计预算；证据为 microcompact 输出总长度。
2. [待验证] callCompactionLlm 的公开入口接受未经 microcompact 的 UnifiedMessage[]；证据为上游 mock 收到的大工具正文。
3. [待验证] UTF-16 分页边界会切开 emoji 代理对；证据为页面首尾孤立代理项。

## 待恢复产物

- [ ] 本文件；完成后从工作区删除。
- [ ] 本轮新增测试及生产修复；作为最终修复保留。
- [ ] /tmp/openawork-tool-context/artifacts；最终验证产物保留。

## Findings
# Debug Journal — 流式连接长时断开
Started: 2026-09-07T00:00:00+08:00
Goal: 定位并修复上游 AI 对话长时间流式连接被主动断开的问题。

## Environment snapshot (Phase 0)
- Runtime: Node.js/TypeScript ESM
- Entry: services/agent-gateway
- Ports / sockets: 用户报告 Gateway http://127.0.0.1:12300
- Git HEAD: $(git rev-parse HEAD), working tree snapshot below
- References read: debugging methodology 00-setup, 02-investigate, runtime node

## Hypotheses
1. [OPEN] 网关/代理存在固定流式超时或 keep-alive 配置，长时间无数据时主动关闭 — 检查 timeout/heartbeat 配置 — 延长超时
2. [OPEN] 上游 SDK 流在长连接中被 AbortSignal/请求生命周期提前取消 — 检查 abort/controller 与 finally — 修正取消
3. [OPEN] 浏览器客户端 SSE/WS 重连或超时逻辑主动断开 — 检查 web-client 与 stream 路由 — 修正客户端

## Artifacts to revert
- [ ] 本日志文件：任务完成后保留作为验证记录
- [ ] `services/agent-gateway/src/v2-runtime/upstream/stream-runner.ts` — 将默认上游流空闲超时从 600000ms 调整为 1800000ms；回滚：手动恢复该常量
- [ ] `/tmp/openawork-stream-baseline` — 基线测试输出
- [ ] `/tmp/openawork-stream-modified` — 修改后测试输出
- [ ] `/tmp/openawork-stream-rollback` — 回滚副本测试输出
- [ ] `MODIFIED_FILE`、`DIFF_FILE`、`VERIFICATION.txt`、`ROLLBACK.sh` — 验证产物

## Findings
- 2026-09-07 10:16:45: `stream-runner.ts` 默认 `DEFAULT_STREAM_IDLE_TIMEOUT_MS = 600_000`，上游无事件 10 分钟即由 `Stream.timeout` 触发 `STREAM_STALL`，表现为流连接中断。
- 2026-09-07 10:22:25: 修改后测试 `Test Files 1 passed (1); Tests 3 passed (3)`，退出码 0；网关 typecheck 退出码 0。
- 2026-09-07 10:20: rollback copy restored to `600_000`, result `rollback_copy_restored=PASS`。

## Final fix
- 将默认上游流空闲截止从 10 分钟提升至 30 分钟；调用方传入的显式 `idleTimeoutMs` 行为不变。
- 回归测试锁定默认值为 1_800_000，并保留原有 watchdog 测试。

## Follow-up investigation — 约 5 分钟断开
- 全仓扫描发现 `services/agent-gateway/src/routes/workflow-llm.ts` 存在第二个精确 300000ms 默认墙钟，适用于团队/工作流的非流式 LLM 调用；已同步提升为 1800000ms并导出常量供测试锁定。
- `packages/opencode-llm/src/stream/processor.ts` 默认 60000ms 仅由该包独立处理器使用，当前网关 native `runUpstreamStream` 路径未实例化 `StreamProcessor`，不是本次主聊天断开来源。
- 网关 SSE 每 10 秒写 keepalive；聊天 WS 客户端 liveness 容忍窗口为 40 秒；未发现 5 分钟连接级主动关闭。
- 修改后联合测试：2 个文件、13 个测试通过；网关 typecheck 退出码 0。

# Debug Journal — 前端实时消息渲染错位
Started: 2026-09-07T10:30:00+08:00
Goal: 定位并修复实时对话消息渲染错位。

## Environment snapshot
- Runtime: Node.js + React 19 + Vite + Vitest + Playwright
- Entry: pnpm --filter @openAwork/web dev
- Git HEAD: 7b1128b83556fbdfda226a506912b3bc9868e616；工作区已有用户修改，不触碰。
- References read: debugging runtime/node、tools/playwright-cli、methodology/00-setup、02-investigate

## Hypotheses
1. [OPEN] 虚拟列表按 group key 缓存高度，实时合并导致 group key/内容改变后偏移未及时重算。
2. [OPEN] 实时虚拟消息按时间戳插入历史列表时，事件卡/新用户消息使其落入错误轮次。
3. [OPEN] 流事件 ordered parts 与快照协调的身份匹配错误，使文本/工具块顺序错位。

## Artifacts to revert
- [ ] 本节日志：调试结束后从 .debug-journal.md 清理，仅保留既有内容。
- [ ] 后续新增失败测试及生产修复：最终修复保留。
- [ ] /tmp 下 Playwright 复现文件与截图：最终清理。

## Findings
- 基线：chat-render-merge、group-render-entries、chat-message-group-list 三文件测试共 15 项通过。
